# MEYLUX V2 — TRANSFER USAGE

## Current Account → Account #2

1. Transfer the `MEYLUX_V2_CROSS_AI_CONTINUATION_PACKAGE.zip` package or place its contents in the destination Project/repository workspace.
2. Provide the destination AI with `docs/continuity/MEYLUX_V2_BOOTSTRAP.md` as the single bootstrap instruction.
3. The destination AI reads the defined bootstrap sequence and returns one concise Continuity Reconstruction Report.
4. If the report is consistent with authoritative repository state, continue the established workflow without manually reconstructing project history.
5. If the destination AI reports a genuine conflict, resolve only that affected conflict through the normal governance process.

The package does not authorize Phase 0. It transfers state; it does not grant new authority.

## Future transfers

At a later gate/phase/step boundary:

1. Update the authoritative repository checkpoint and relevant state artifacts through the normal governed workflow.
2. Ensure the latest verified commit and active Task Order/evidence chain are recorded.
3. Reuse this bootstrap package structure.
4. Update only the state manifest and any boundary-specific continuation references.
5. Transfer the package plus repository reference.
6. Require one concise Continuity Reconstruction Report.
7. Continue when authoritative state and existing authorization are consistent.

Never use an old transfer package to override a newer repository state. The repository wins.
