# MEYLUX V2 — TO-P0-002

## PHASE 0 SEQUENCE FORMALIZATION

**Task Order ID:** `TO-P0-002`
**Phase:** `PH-P0`
**Task:** Phase 0 Sequence Formalization
**Issuer:** CONTROL / REVIEWER (`ROL-V2-001`)
**Recipient:** PRODUCER / ARCHITECT-BUILDER (`ROL-V2-002`)
**Status:** AUTHORIZED TO EXECUTE

---

## 1. PURPOSE

Formally extract and document the complete Phase 0 roadmap/sequence that is supportable from authoritative project artifacts and establish the next valid Phase 0 Step from the resulting sequence and the current verified project state.

This Task Order is a **sequence/formalization task only**.

It does not authorize execution of any future Phase 0 Step and does not itself ratify or redesign the Master Architecture.

---

## 2. CURRENT VERIFIED INPUT STATE

Use the repository's current verified state as an input, including at minimum:

```text
PH-P0 = ACTIVE
STEP-P0-001 = COMPLETE / VERIFIED
```

The following historical identity is immutable for this task:

```text
STEP-P0-001 — Master Architecture Reconciliation
= COMPLETE / VERIFIED
```

Do not rename, renumber, recreate, reinterpret, or re-execute `STEP-P0-001`.

Do not assume that `STEP-P0-002 — Constitution Ratification` is the next valid Step. Determine the Next Valid Step from authoritative sequence evidence and current verified state.

---

## 3. AUTHORITATIVE INPUTS

Inspect and reconcile, as applicable:

1. `docs/architecture/MASTER_ARCHITECTURE_V2.md`
2. `docs/phases/PH-P0.md`
3. `docs/registry/phases.yaml`
4. `docs/state/CURRENT_CHECKPOINT.json`
5. Existing Phase 0 Task Orders, Build Reports, Audit Reports, and other Phase 0 evidence.
6. `docs/project-formation/PROJECT_FORMATION_STATUS.md`
7. Applicable governance, decision, artifact-protocol, continuity, registry, and change-control artifacts.
8. Any higher-level authoritative project artifact that explicitly defines or constrains the Phase 0 lifecycle or sequence.

Use artifact status and authority hierarchy when reconciling evidence. Draft/reference material must not silently override verified or otherwise higher-authority project state.

---

## 4. REQUIRED WORK

### 4.1 Extract the Phase 0 Sequence

Determine every Phase 0 Step that can be established from authoritative evidence, from the first Step through the final Step, to the maximum extent supportable by the available sources.

For every established Step, record:

- Step ID
- Title
- Order
- Purpose
- Primary Work
- Expected Output / Result
- Dependencies / Ordering Constraints

Do not create a Step merely because it appears useful or because a sequence appears incomplete.

### 4.2 Determine the Final Step and Completion Boundary

Determine the Phase 0 Final Step and the Phase 0 Completion Boundary if, and only if, authoritative evidence establishes them with sufficient confidence.

If the final Step or completion boundary cannot be established, report the exact unresolved conflict or missing authority instead of guessing.

### 4.3 Reconcile Against Current Verified State

Map the extracted sequence against the current verified state.

At minimum, establish:

```text
CURRENT VERIFIED STEP
NEXT VALID DEFINED STEP
REMAINING PHASE 0 STEPS
FINAL STEP / COMPLETION BOUNDARY (if established)
```

The resulting sequence must preserve the already executed and verified history.

### 4.4 Determine the Next Valid Step

Determine the Next Valid Step strictly from:

```text
FORMALIZED SEQUENCE
        +
CURRENT VERIFIED STATE
        +
VALID DEPENDENCIES / ORDERING CONSTRAINTS
```

If evidence establishes:

```text
STEP-P0-002 — Constitution Ratification
```

as the next valid Step, report it as such.

If another Step is established instead, report that Step.

If the evidence conflicts or is insufficient, stop only that determination and report the conflict rather than selecting a winner by personal interpretation.

---

## 5. CONFLICT HANDLING

If authoritative or potentially authoritative inputs conflict, do not resolve or override the conflict by Producer judgment.

For each material unresolved conflict, report exactly:

```text
Conflict:
Affected Artifacts:
Decision Required:
Required Authority:
```

Preserve all existing historical identity and verified state.

Do not use this Task Order as authority to ratify Architecture, alter Governance, reopen a Gate, or rewrite project history.

---

## 6. EXPLICIT NON-CHANGES / PROHIBITIONS

The Producer MUST NOT:

- execute any future Phase 0 Step;
- issue a Task Order for any future Step;
- implement future Step work;
- ratify or freeze the Master Architecture;
- redesign the Architecture;
- invent a Step ID or Step definition;
- invent a sequence item solely to fill a gap;
- retroactively rename, renumber, rewrite, recreate, or re-execute `STEP-P0-001`;
- modify `G-0`;
- modify `G-0R`;
- reopen `PP-00 … PP-12`;
- reopen F003;
- alter Phase 0 authorization;
- alter V1/VPS/runtime state;
- perform Market or Trading operations;
- create a new governance layer;
- create a new sequence/continuity subsystem;
- create a new Stable ID merely to solve a conflict;
- silently modify the Master Architecture to conceal a discrepancy.

Sequence formalization is not execution authorization.

---

## 7. GOVERNANCE / AUTHORITY BOUNDARY

The Producer may extract, analyze, and document evidence within this Task Order.

The Producer may not make an architecture or governance decision that requires formal ratification.

If formalization of any portion of the sequence requires an Architecture Decision, ACR, change-control approval, or other higher-authority governance decision, stop that portion and report:

```text
Conflict / Required Decision
Affected Artifacts
Decision Required
Required Authority
```

Do not invent an ADR/ACR ID or authority.

---

## 8. REQUIRED OUTPUT

Produce one auditable formal sequence output covering at minimum:

```text
PHASE 0
  ↓
COMPLETE FORMALIZED SEQUENCE
  ↓
CURRENT VERIFIED STEP
  ↓
NEXT VALID DEFINED STEP
  ↓
REMAINING DEFINED STEPS
  ↓
FINAL STEP / COMPLETION BOUNDARY (if established)
```

The output must also state the evidence basis for each established Step and identify any unresolved conflict or authority dependency.

The output must be suitable for subsequent transition decisions to rely on the formalized sequence and current verified state without re-analyzing the entire Phase 0 roadmap, subject to normal controlled change if the authoritative sequence itself later changes.

---

## 9. OUTPUT ARTIFACT / BUILD REPORT

The Producer must return the required formal sequence artifact and a truthful `BUILD-REPORT` identifying:

- `TO-P0-002`;
- actual work performed;
- authoritative sources inspected;
- extracted sequence;
- current verified state used;
- Next Valid Step determination;
- final Step / completion boundary determination, if established;
- conflicts/open questions/deviations;
- actual evidence;
- work explicitly not performed;
- final Producer disposition.

No unexecuted check may be represented as executed or verified.

---

## 10. ACCEPTANCE CRITERIA

This Task Order is acceptable only if the Producer output demonstrates that:

1. The Phase 0 sequence was extracted from authoritative evidence rather than invented.
2. `STEP-P0-001` remains exactly the historical/verified Step already established.
3. No future Step was executed or implicitly activated.
4. Every established Step has the required sequence metadata.
5. The current verified state is explicitly mapped to the sequence.
6. The Next Valid Step is explicitly determined, or the exact blocking conflict is reported.
7. The Final Step and Phase 0 Completion Boundary are stated only when sufficiently established by evidence.
8. Conflicting artifacts are not silently overridden.
9. No prohibited governance, architecture, Gate, V1/VPS, runtime, market, or trading change occurred.
10. The result is traceable and auditable from repository evidence.

---

## 11. STOP CONDITIONS

Stop the affected determination and report the required conflict if:

- no authoritative evidence can establish a required sequence element;
- two authoritative requirements materially conflict;
- formal architecture/governance ratification is required;
- required authority is absent or undefined;
- resolving the issue would require changing a Stable ID or historical verified state;
- completing the task would require any prohibited change listed above.

Do not solve a stop condition by assumption.

---

## 12. EXECUTION BOUNDARY

This Task Order formalizes the roadmap only.

It does NOT authorize:

```text
STEP-P0-002 execution
or
any other future Step execution
```

After this Task Order is audited and accepted, any future Step must proceed through the normal controlled lifecycle:

```text
FORMALIZED SEQUENCE
        ↓
CURRENT VERIFIED STATE
        ↓
NEXT VALID STEP
        ↓
SEPARATE TASK ORDER FOR THAT STEP
        ↓
PRODUCER EXECUTION
        ↓
BUILD-REPORT
        ↓
CONTROL AUDIT / VERIFICATION
        ↓
NEXT SEQUENCE TRANSITION
```

---

## 13. PRODUCER INSTRUCTION

Execute only this formalization task.

Do not execute the Phase 0 roadmap itself.

Do not pre-authorize or pre-build the next Step.

Return the formal sequence and truthful evidence through the established Producer → BUILD-REPORT → CONTROL review cycle.
