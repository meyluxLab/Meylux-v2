# BR-P2-008 — Corrected Producer Build Report

**Project:** MEYLUX V2  
**Role:** ROL-V2-002 — PRODUCER / ARCHITECT-BUILDER  
**Task Order:** TO-P2-008  
**Boundary:** PH-P2 / STEP-P2-006 / A-P2-BINANCE  
**Producer lifecycle at delivery:** IMPLEMENTED / TESTED / PASSED  
**CONTROL state at delivery:** READY FOR INDEPENDENT AUDIT

## 1. Implementation

Producer correction head: `b5491d9dee7a5e1e1dc6cab4bd670ca18710999c`.

The correction was delivered through PR #47 and subsequently merged by CONTROL as merge commit `14f00e25893fcf73c6c5d881fb86852a8091f909`.

Exactly four implementation/test files changed relative to the pre-correction integration baseline `bc2227875e6024ea8ff7762203ebf0e4a9b9b56a`:

- `contracts/normalization.py`
- `src/meylux/acquisition/binance.py`
- `tests/test_acquisition/test_binance_adapter.py`
- `tests/test_acquisition/test_binance_finality.py`

The implementation:
- preserves Binance WebSocket `k.x` as the authoritative explicit finality signal;
- rejects missing/non-boolean `k.x`;
- validates `k.t`, `k.T`, `T > t`, symbol consistency, and interval;
- removes historical REST synthetic finality and carries `x=None`;
- validates REST close time without deriving finality from receipt time;
- preserves the existing minimum seven-field REST adapter policy;
- explicitly rejects unsupported providers at the candle normalization mapping boundary;
- introduces no `knowledge_time` field or inference;
- does not modify MEXC mapping behavior;
- does not modify persistence, migrations, VPS configuration, or G-4 quantitative identity algorithms.

## 2. Execution Evidence

Repository-hosted GitHub Actions executed the corrected head.

**CI Core:** Run #1325 / Run ID `36132680775` / Job `repository-foundation` / Job ID `108063348102` — SUCCESS.

Verified job steps include compile, Binance acquisition tests, P2-004 tests, P2-005 tests, P3-007 tests, P3-008 tests, and foundation regression.

**CI Docker Foundation:** Run #327 / Run ID `36132680788` / Job `docker-foundation` / Job ID `108063348410` — SUCCESS.

Producer reported:
- Binance adapter: 20/20 PASS
- Binance finality: 27/27 PASS
- full repository regression: 542/542 PASS
- final exit status: 0

The CI metadata independently confirms successful completion of the relevant jobs. No VPS execution, live Binance acquisition, migration, production mutation, or historical database rewrite was performed.

## 3. Rule-5 Coverage

The finality suite contains 27 cases covering explicit true/false, missing/non-boolean finality, missing/malformed open and close times, temporal contradiction, provider metadata mismatch, unsupported provider, REST no-finality behavior, receipt/local-clock independence, event/close/receipt distinction, knowledge_time absence, non-final→final, final→conflicting non-final, replay identity, deterministic canonical serialization, historical row-shape boundaries, malformed REST close time, canonical contract shape, and Binance isolation.

## 4. Identity / G-4

The Producer explicitly disclosed that AcquisitionEnvelope identity bytes include payload, so removing synthetic historical `x=True` changes newly generated raw-observation identity material.

No identity algorithm was changed, no existing historical identity was rewritten, no G-4 quantitative identity algorithm or canonical candle identity construction was changed, and no existing G-4 population was rewritten.

This distinction was independently reviewed by CONTROL and accepted as outside the G-4 identity-change prohibition because the governed change does not modify the G-4 identity algorithm or existing G-4 population.

## 5. Historical REST Disposition

Binance REST does not expose the WebSocket `k.x` finality flag. The corrected path therefore does not manufacture provider-final status. Raw REST rows remain preserved; `x=None` is carried forward and canonical normalization rejects unavailable `is_closed` rather than fabricating `True`.

The existing adapter's seven-field minimum-row policy is preserved. A malformed close-time field is now explicitly classified.

## 6. Scope / Lifecycle

No MEXC implementation, A-P3, Routes B/C/D, P5-003, Futures, Forex, truth-layer, frozen-architecture, migration, VPS mutation, or closure synchronization was performed by Producer.

Producer did not declare VERIFIED or CLOSED.

## 7. Producer Conclusion

The authorized implementation and repository execution boundary was completed and handed to CONTROL for independent verification.
