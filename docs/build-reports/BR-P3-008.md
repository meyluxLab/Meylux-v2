# BR-P3-008 — Authoritative Persistence, Event Handoff & G-3 Verification

**Step:** STEP-P3-008  
**Task Order:** TO-P3-008 Revision 1.1  
**Producer:** ROL-V2-002  
**CONTROL:** ROL-V2-001  
**Lifecycle:** PRODUCED / TESTED / UNVERIFIED  
**Verification:** UNVERIFIED  
**Closure:** OPEN

## 1. Producer Boundary

This report records Producer implementation, automated testing, development/integration preparation, deployment artifacts and evidence. Production VPS mutation, official EXEC-LOG, independent runtime verification, G-3 verification, registry/checkpoint synchronization and Phase 3 closure remain CONTROL-owned.

## 2. Implementation Commit Context

Post-correction implementation commit: `99495e9f52c0b4f8e1442f17d1392b3694cfd060`.

Relevant implementation commits include:
- `1772ebad0b7fe5456d623a6d94a2e4141a2114f1`
- `1e2b4a434d41218b65b8a834d942bae88ebf48ef`
- `21b614a29e5f3c03a9e14769b3fc7ded289984d4`
- `1b106e64911252f80fe9e6a0dccc662624d32372`
- `77b305a6b14c86d1b1cb621d9a38524cc137ec8b`
- `df5611f7b70ea481a6f32be5cb4371e38499eae6`
- `25295fd755817c2cd7eba4f2128cdc4ec04f9183`
- `b954a26534b87750d1648307965fd2bd02fc83b9`
- `3de782d31602898267f57b0d6accc085e9a790f1`
- `51ed2ce771e7e7ee2408aa4fd65302f688a7e862`
- `ec7ae0030c7773be77258a8b0b0679f9107758d9`
- `20e9189c56723c8669c5fefc0f435b21e4eda444`
- `6c5f8a56d7832763717ce79a81cfad468d71e06c`

Correction commits `46ba82cf4ba33a711d790a1432501aa3247f7c44` and `b95e123fa1888072f9c8d7deb75b7880f3a3bc16` corrected an implementation syntax defect identified by CI; `6c5f8a56d7832763717ce79a81cfad468d71e06c` removed the duplicate import. No production VPS action was performed by the Producer.

## 3. Changed / Created Paths

- `contracts/event.py`
- `src/meylux/persistence/__init__.py`
- `src/meylux/persistence/canonical.py`
- `src/meylux/persistence/event_handoff.py`
- `src/meylux/persistence/factory.py`
- `src/meylux/runtime/p3_008_vertical_slice.py`
- `migrations/versions/0003_canonical_persistence_event_outbox.sql`
- `infrastructure/postgres/migrate.sh`
- `infrastructure/compose/docker-compose.yml`
- `pyproject.toml`
- `.github/workflows/ci-core.yml`
- `.github/workflows/ci-docker.yml`
- `tests/test_p3_008_persistence.py`
- `tests/test_p3_008_persistence_integration.py`

## 4. Architectural Mapping

The implementation preserves the verified P3-001 through P3-007 contract boundary and does not introduce Phase 4 analytical, intelligence, opportunity, arbitrage or trading behavior.

The persistence boundary accepts only a canonical-eligible `VALID` quality assessment with complete provenance, source identity and lineage references. Provider wire structure remains upstream of normalization.

The event boundary is a normalized provider-neutral envelope on `stream:canonical:market_events`.

## 5. Persistence Design

Migration `0003_canonical_persistence_event_outbox.sql` establishes:
- `canonical_instruments`
- `canonical_candles`
- `canonical_trades`
- `canonical_orderbook_depth`
- `canonical_derivatives`
- `data_quality_logs`
- `canonical_event_outbox`

Canonical records contain deterministic record/event identity, canonical payload JSON, canonical bytes, quality state/score, provenance, source identity, lineage parent identity and UTC persistence time.

Canonical history is append-only at both privilege and trigger boundaries. Application runtime privileges allow SELECT/INSERT but not UPDATE/DELETE/TRUNCATE on canonical tables.

The canonical insert and outbox insert occur inside one database transaction. Duplicate record identity is idempotent and identity conflicts are rejected.

The outbox is mutable only for publication acknowledgement and is ordered by a database-generated sequence.

## 6. Event-Handoff Design

The relay consumes pending outbox rows in sequence order and publishes normalized fields only. It does not expose provider-specific wire structure.

Redis publication uses a stable event identity and an atomic Lua check/publish/remember operation. Redis 7 compatibility is preserved; newer Redis 8.6-only IDMP syntax is not required.

The stream is bounded with approximate MAXLEN retention. Idempotency keys have bounded TTL. The authoritative database outbox remains the recovery source when Redis is unavailable or a publication acknowledgement is lost.

A publication failure raises an explicit handoff error and leaves the outbox publication marker unchanged.

## 7. Quality / Lineage Preservation

The persistence boundary requires:
- `DataQualityState.VALID`;
- canonical eligibility;
- provenance ID;
- source record ID;
- lineage parent ID.

The transaction also records the quality state/lifecycle, score, reason codes, validation result, lineage references and payload fingerprint in `data_quality_logs` when a quality assessment is supplied.

Non-promotable evidence is rejected before canonical insertion.

## 8. Runtime / Deployment Artifacts

- PostgreSQL runtime dependency: `asyncpg`.
- API Compose runtime receives DB application credentials and Redis endpoint without embedding secret values in Git.
- Migration harness executes `0003_canonical_persistence_event_outbox.sql` after the verified `0001` and `0002` migrations.
- `src/meylux/persistence/event_handoff.py` is the bounded one-shot outbox relay.
- `p3_008_vertical_slice.py` is a controlled real-data preparation/verification runner. It selects only an existing AVAILABLE raw acquisition record; it has no fabricated fallback path. Production execution remains CONTROL-owned.

## 9. Automated Coverage

Dedicated tests cover:
- canonical non-promotable state rejection;
- quality/lineage preservation;
- deterministic event serialization;
- missing lineage rejection;
- migration object/append-only declarations;
- migration ordering;
- Compose runtime wiring;
- atomic persistence and quality logging;
- duplicate replay behavior;
- ordered event relay;
- transport failure isolation.

CI Core was extended to run `test_p3_008*.py`. Docker CI was extended to validate migration `0003`, canonical objects and application privilege boundaries.

Fresh accepted CI evidence for the JSONB raw-row → AcquisitionEnvelope correction state:
- CI Core workflow, Run #617, Run ID `35373967430`, job `repository-foundation`, head `893e5173be03ebc7ec74460ab18ccae9f289f50f`: **success**. The P3-008 persistence/event suite completed successfully, and the full foundation regression completed successfully with **280 tests**.
- CI Docker Foundation workflow, Run #135, Run ID `35373967459`, job `docker-foundation`, head `893e5173be03ebc7ec74460ab18ccae9f289f50f`: **success**. The full Docker foundation test suite completed successfully with **280 tests**; runtime contracts packaging, database startup, migrations, canonical persistence objects, application privilege checks and UTC verification all completed successfully.
- The accepted Fresh CI evidence above is the final CI evidence for this Producer correction state. Earlier Run #612/#130, #613/#131, #614/#132 and #615/#133 failures were intermediate correction/fixture failures and are retained only as historical execution evidence, not as final acceptance evidence.
- CI conclusion at the Producer documentation boundary: **CI VERIFIED = SUCCESS for the corrected JSONB boundary state**. This does not establish Production runtime verification, G-3, or Step closure.
## 10. Failure / Recovery Matrix

Implemented handling:
- database transaction failure → transaction rollback; no canonical/outbox partial commit;
- duplicate canonical record → idempotent no-op when event identity agrees;
- canonical identity conflict → explicit error;
- Redis publication failure → explicit EventHandoffError; outbox remains pending;
- publisher crash after Redis publication before DB acknowledgement → stable Redis idempotency key prevents a duplicate within the bounded deduplication window; outbox remains recoverable;
- Redis restart/loss of ephemeral idempotency keys → authoritative outbox permits replay, with stable event identity for downstream idempotency;
- stream growth → bounded MAXLEN retention;
- relay batch size → bounded 1..1000;
- canonical quality rejection → no canonical persistence.

## 11. Security

No credentials are committed by the implementation. Runtime DB access uses the least-privilege application role. Migration authority remains separated from application DML privileges. Secret values are never recorded in this report.

No public database listener or trading/capital functionality was added by this Step.

## 12. Deferred / Unsupported

- Production VPS deployment/execution, official EXEC-LOG, independent runtime verification, G-3 verification, registry/checkpoint synchronization and Phase 3 closure remain CONTROL-owned.
- Production synchronization to the merged commit `2f61be905259c7dc5ce5649b19931a83cc9591c2`, application image rebuild/recreate and migration verification were performed by CONTROL and are not Producer verification claims.
- The prior Production `ModuleNotFoundError: No module named 'contracts'` defect was not reproduced after the accepted runtime-packaging correction.
- CONTROL subsequently established one real governed Binance Spot `AVAILABLE / TRADE` raw acquisition event through the existing Phase 2 acquisition path and successfully read it back; no synthetic or fallback market data was used.
- The controlled P3-008 vertical-slice retry then reached the raw-row → `AcquisitionEnvelope` reconstruction boundary and exposed `TypeError: payload must be a mapping` because PostgreSQL `jsonb` was returned in a runtime representation that was not a Mapping.
- This Producer correction decodes string/bytes JSONB object representations into a Mapping before `AcquisitionEnvelope` construction, preserves Mapping values, and explicitly rejects malformed JSON, non-object JSON and unsupported runtime representations. The envelope contract remains unchanged and continues to require a Mapping.
- The corrected boundary is covered by deterministic regression tests and accepted Fresh CI in Section 9.
- Production controlled persistence, canonical read-back, event receipt, failure/recovery runtime evidence and G-3 remain unestablished pending CONTROL re-execution against the corrected implementation.
- No synthetic or fabricated market data is used or proposed as a bypass.
- Roadmap performance target `>5,000 normalization events/sec/core` is not measured by this Producer cycle and remains unverified.
- A downstream Phase 4 consumer is not implemented; the normalized stream is the P4-ready handoff boundary only.
## 13. Governance Note

The Producer does not modify the final checkpoint, closure registries, README closure state or Change Ledger transition. These remain CONTROL responsibilities.

## 14. Producer-to-CONTROL Handoff

The Producer lifecycle boundary is reached: implementation, automated testing, development/integration evidence and Build Report are complete. This report must not be interpreted as Step verification or closure.

## 17. Repository Hygiene / Correction Record

One Producer file-creation operation initially omitted the feature-branch argument and created `src/meylux/runtime/p3_008_vertical_slice.py` on `main` as commit `cfee73b157e9d20457fa43ece9d10ac8ed15303d`. The file contained no production execution or secret data. CONTROL authority was not exercised and no VPS action occurred. The accidental main-branch mutation was immediately removed by revert commit `9bc97f25dd9d01448246c48f567024533aae2571`. The final Producer branch contains the corrected file under the authorized feature branch. The post-correction CI evidence is intentionally not claimed until a fresh run completes.

This event is recorded explicitly for auditability; it does not constitute a production deployment or verification event.


## 18. CONTROL Correction Cycle — F1/F2/F3

CONTROL identified three Producer corrections. F1 is corrected by removing the duplicate `data_quality_logs` execution from `CanonicalPersistence.persist()` while preserving the existing transaction boundary. The regression test now executes `CanonicalPersistence.persist()` against a deterministic in-memory async connection double and asserts exactly one `data_quality_logs` INSERT execution plus exactly one transaction entry; it no longer relies on source-text counting. F2 is corrected throughout this report: the actual relay path is `src/meylux/persistence/event_handoff.py`, and no obsolete relay implementation path remains. F3 remains accepted as previously corrected in `docs/registry/artifacts.yaml`.

Actual post-rejection correction commits:
- F1 implementation: `a0fd0caa2202b7db6392b9cf160bc7c5d84784c0`
- F1 deterministic runtime regression test: `691a3bc750685423a734c0371820e298cf1dc37c`

No nonexistent or unverified correction SHA is used as evidence. Fresh post-correction CI is now complete and successful as recorded in Section 9.


## 19. Production Runtime Packaging Correction — Contracts Dependency

CONTROL independently executed the authorized Production vertical-slice runner after synchronization to the corrected merged state and application rebuild. The earlier `ModuleNotFoundError: No module named 'contracts'` defect was not reproduced.

The accepted Producer correction remains:
- `infrastructure/docker/Dockerfile` explicitly copies `contracts/` into `/app`;
- runtime `PYTHONPATH=/app:/app/src` makes the repository-level `contracts.*` dependency importable;
- `.github/workflows/ci-docker.yml` triggers on `contracts/**` changes and executes a Docker-image import invariant covering the required contracts modules and `meylux.runtime.p3_008_vertical_slice`.

Correction commits:
- Runtime image/package boundary: `e391a0465cb2ecb9e76b484d847f63cd23c6bca1`
- Docker/runtime packaging coverage: `2a03c3e8e2e3dec15db82bbabdd73be7b2f125c3`

Final accepted Fresh CI evidence for that correction:
- CI Core #594 / Run ID `35365834192`
- CI Docker Foundation #124 / Run ID `35365833834`
- corrected PR head: `1ff0dc64bde9056053cc3a4beddc92f26401085c`
- both workflows: **success**

After CONTROL established a real governed AVAILABLE raw acquisition event, the authorized vertical-slice retry progressed beyond the former zero-row boundary and exposed the JSONB reconstruction defect recorded in this correction cycle: `TypeError: payload must be a mapping` at `envelope_from_row()`.
 
Producer correction:
- `src/meylux/runtime/p3_008_vertical_slice.py` now decodes PostgreSQL `jsonb` string/bytes representations with `json.loads`, requires the decoded value to be a Mapping, preserves already-mapped values, and rejects malformed/non-object/unsupported representations explicitly.
- `AcquisitionEnvelope` contract validation was not weakened. PostgreSQL `jsonb` schema remains unchanged.
- Runtime database/Redis imports were moved into `main()` so the pure raw-row reconstruction boundary can be tested in the repository Core environment without changing production dependencies.
- Deterministic regression coverage exercises JSON object strings, bytes, Mapping values, JSON arrays, malformed JSON and unsupported runtime values.
 
Correction implementation commits:
- JSONB reconstruction: `20609b63ca07a13735f9a1f16a9c5109aaf65696`
- Pure-boundary testability/runtime import isolation: `0eb945150fef941a2d23adcd169babb3a755bbb3`
- Final boundary test fixture corrections: `893e5173be03ebc7ec74460ab18ccae9f289f50f`
 
Final accepted Fresh CI:
- CI Core #617 / Run ID `35373967430`
- CI Docker Foundation #135 / Run ID `35373967459`
- final corrected PR head: `893e5173be03ebc7ec74460ab18ccae9f289f50f`
- both workflows: **success**
 
The correction does not establish Production canonical persistence, event receipt, recovery, G-3 or Step closure. No Production EXEC-LOG, G-3 state, CURRENT_CHECKPOINT closure state, CONTROL verification state, or Phase 3 closure state is modified by this Producer correction.
