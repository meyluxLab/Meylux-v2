# BR-P2-004 — Live Collector, Raw/Staging Persistence & Replay Safety

**Task Order:** `TO-P2-004`  
**Step:** `STEP-P2-004`  
**Producer:** `ROL-V2-002 — PRODUCER / ARCHITECT-BUILDER`  
**Status:** `IMPLEMENTED / TESTED / UNVERIFIED`  
**CONTROL verification:** PENDING

## Scope

Implemented the authorized Phase 2 Step 4 collector integration, provider-neutral Binance/MEXC routing, raw/staging persistence, migration integration, bounded queue/backpressure, idempotent persistence, replay safety, bounded persistence recovery, source-sequence preservation, provider isolation, evidence-compatible observability, and deterministic tests.

Preserved `CMP-P2-001`, `CTR-P2-001`, verified Binance/MEXC acquisition boundaries, PostgreSQL/TimescaleDB, Redis/ARQ, frozen architecture, Phase 2 ownership, governance/security boundaries, and V1 isolation.

No P2-005/P2-006, Phase 3/4, AI/specialist intelligence, production deployment, or trading/account/capital functionality was introduced.

## Corrections

`infrastructure/postgres/migrate.sh` now applies `0001_database_foundation.sql` before `0002_raw_acquisition_staging.sql`.

The Docker Foundation readiness probe now checks TCP readiness with `pg_isready -h 127.0.0.1 -U meylux_admin -d meylux_ci`, resolving the previously observed socket-vs-TCP readiness mismatch.

`tests/test_acquisition/__init__.py` was added for unittest discovery. The P2-004 migration-path assertion was corrected from the incorrect `ON CONFLICT (event_id)` expectation to the governed `ON CONFLICT (version)` registration semantics. The replay test was corrected to exercise replay after collector lifecycle reset.

Correct P2-004 discovery exposed three Binance test failures. Reconciliation determined: REST `ProviderError.retryable` remains valid after bounded local retries because the contract does not require exhaustion to flip the semantic field; `%40` is the valid URL-encoded representation of `@` and the test now validates the decoded stream value; telemetry is emitted through the existing structured path on `bootstrap()`, so the test now exercises that governed path. No Binance implementation, `CTR-P2-001`, or provider architecture was changed.

## Final deterministic execution evidence

Current PR #15 HEAD:

`c961630eb1e9a0b7df0c5cf8530cf45ae81e7021`

This is the final implementation/evidence head independently identified by CONTROL for the current correction cycle.

### CI Core

Run `#253`, Run ID `34637929282`, job `repository-foundation` → `SUCCESS`.

Final successful execution included:

- `python -m compileall -q src config tests`
- explicit Binance acquisition tests → `Ran 16 tests ... OK`
- explicit P2-004 acquisition tests → `Ran 16 tests ... OK`
- full repository regression → `Ran 154 tests ... OK`

The full regression explicitly discovered and executed both:

- `test_acquisition.test_binance_adapter`
- `test_acquisition.test_p2_004_collector_persistence`

Therefore P2-004 execution is demonstrably visible rather than inferred from workflow status alone.

### CI Docker Foundation

Run `#56`, Run ID `34637929302`, job `docker-foundation` → `SUCCESS`.

Final successful execution passed:

- Compose validation;
- foundation image build;
- foundation self-checks;
- database startup;
- TCP database readiness;
- database migration harness;
- database foundation/schema verification;
- cleanup.

The migration harness and schema verification therefore completed successfully on the final PR HEAD.

## Prior-cycle evidence

The following evidence is retained only as historical/prior-cycle evidence and is not presented as final current-head evidence:

- CI Core `#252` / Run ID `34637776545` → `SUCCESS`, against prior head `4cb1dcf64b7e3dd29dd116f1b158f6404cb364ce`;
- CI Docker Foundation `#55` / Run ID `34637776512` → `SUCCESS`, against the prior correction cycle.

These executions supported the correction process but are superseded for final-head traceability by CI Core `#253` and Docker Foundation `#56` on `c961630eb1e9a0b7df0c5cf8530cf45ae81e7021`.

## Changed paths

- `.github/workflows/ci-core.yml`
- `.github/workflows/ci-docker.yml`
- `docs/build-reports/BR-P2-004.md`
- `docs/registry/phase2-artifacts.yaml`
- `infrastructure/postgres/migrate.sh`
- `migrations/versions/0002_raw_acquisition_staging.sql`
- `src/meylux/acquisition/__init__.py`
- `src/meylux/acquisition/collector.py`
- `src/meylux/acquisition/models.py`
- `src/meylux/acquisition/persistence.py`
- `src/meylux/acquisition/transport.py`
- `tests/test_acquisition/__init__.py`
- `tests/test_acquisition/test_binance_adapter.py`
- `tests/test_acquisition/test_p2_004_collector_persistence.py`

## Evidence boundary

No new live Binance/MEXC network execution, V2 VPS migration, collector activation, or production deployment is claimed. CI evidence is repository execution evidence, not live-provider evidence.

## Producer disposition

All requested deterministic repository and Docker Foundation evidence is successful on the final PR HEAD. Final governance disposition remains the responsibility of independent CONTROL verification.

**Governed state: `TO-P2-004 / STEP-P2-004 → IMPLEMENTED / TESTED / UNVERIFIED`**

The Producer does not claim `VERIFIED` or `COMPLETE`.
