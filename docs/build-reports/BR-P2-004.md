# BR-P2-004 — Live Collector, Raw/Staging Persistence & Replay Safety

**Task Order:** `TO-P2-004`
**Step:** `STEP-P2-004`
**Producer:** `ROL-V2-002`
**Status:** `IMPLEMENTED / TEST EXECUTION PENDING / UNVERIFIED`
**CONTROL verification:** PENDING

## Correction cycle

The Producer implemented the four concrete correction requirements identified by CONTROL's independent audit of PR #15.

### 1. Migration integration — corrected

`infrastructure/postgres/migrate.sh` now executes `0001_database_foundation.sql` followed by `0002_raw_acquisition_staging.sql` through the existing migration path. No migration framework or database foundation redesign was introduced.

### 2. Persistence failure recovery — corrected

`AcquisitionCollector` now performs a bounded synchronous persistence retry policy. If all bounded attempts fail, the exact immutable `AcquisitionEnvelope` is retained in a bounded recovery buffer and exposed through `recovery_items`; `replay_recovery()` reuses the same persistence path for explicit replay. Recovery-capacity exhaustion raises `PersistenceRecoveryOverflow` rather than silently dropping the event. Provider payloads and credentials remain outside observability output.

### 3. Required deterministic coverage — expanded

The P2-004 test module now covers:

- Binance and MEXC routing through the provider-neutral collector boundary;
- raw/staging persistence and retrieval;
- deterministic canonical serialization and identity;
- duplicate/idempotency and replay safety;
- source sequence preservation and forwarding of existing reconnect controls;
- provider failure isolation;
- bounded queue/backpressure;
- persistence retry and terminal recovery retention/replay;
- malformed acquisition-state rejection and valid INVALID-state evidence;
- migration-path/schema validation;
- finite lifecycle and bounded shutdown;
- queue bridge preservation of identity/canonical evidence.

### 4. Current CI failure — addressed

CONTROL reported CI Docker Foundation Run #29 failing at `Run foundation self-checks`, while CI Core Run #235 succeeded. The workflow definition showed that Docker Foundation invoked the same unittest discovery command without the `PYTHONPATH=src` environment explicitly supplied by CI Core. The correction aligns that self-check environment with the existing repository foundation convention. The Docker workflow additionally validates the P2-004 migration record and raw/staging table/schema after the existing migration harness runs.

## Exact correction commit

`a4dc17998702b043bc94b5fbe4c7c8797aa2158d` implemented the first correction pass. The subsequent refinement commit for replay behavior and coverage is the current PR head after this report is regenerated by CI.

## Execution evidence

No test or CI result for the corrected head is claimed yet. The repository will provide the authoritative execution evidence through the newly triggered CI workflows.

The required commands represented by CI Core are:

```text
python -m compileall -q src config tests
python -m unittest discover -s tests -v
```

The Docker Foundation workflow additionally executes:

```text
docker compose -f infrastructure/compose/docker-compose.yml config --quiet
docker build -f infrastructure/docker/Dockerfile -t meylux-v2-foundation:ci .
python -m unittest discover -s tests -v
... existing migration harness ...
... P2-004 schema verification ...
```

No V2 VPS runtime migration, collector activation, or live Binance/MEXC execution is claimed. The previously observed SentinelX host remains insufficient to establish the V2 workspace location.

## Scope / governance

Preserved: `CMP-P2-001`, `CTR-P2-001`, verified Binance/MEXC adapters, PostgreSQL/TimescaleDB architecture, Redis/ARQ conventions, `DOC-V2-ARCH-001`, Phase 2 ownership, governance/security boundaries.

Out of scope: P2-005, P2-006, Phase 3 validation/normalization, Phase 4 quantitative/market-structure, AI/specialist intelligence, production deployment, V1 activity, trading/account/capital functionality, unrelated infrastructure cleanup.

## Producer disposition

**`IMPLEMENTED / TEST EXECUTION PENDING / UNVERIFIED`**

CONTROL must independently re-audit and determine verification disposition. Producer does not claim `VERIFIED` or `COMPLETE`.
