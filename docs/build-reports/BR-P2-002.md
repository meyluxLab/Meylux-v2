# BR-P2-002 — Binance Acquisition Adapter

**Build Report SID:** `BR-P2-002`  
**Task Order:** `TO-P2-002`  
**Phase:** `PH-P2`  
**Step:** `STEP-P2-002`  
**Producer:** `ROL-V2-002 — PRODUCER / ARCHITECT-BUILDER`  
**Status:** `IMPLEMENTED / TESTED / UNVERIFIED`

## 1. Correction Scope

This revision records the bounded corrections directed by `CONTROL` under `AR-P2-AUDIT-002`.

Only `TO-P2-002 → STEP-P2-002` was changed. The provider-neutral acquisition architecture and `CTR-P2-001 — AcquisitionEnvelope` were reused without redesign.

## 2. F1 — WebSocket Reconnect Bound

`src/meylux/acquisition/binance.py` was corrected so that both clean WebSocket termination and exceptional connection failure consume reconnect attempts.

For one `stream()` invocation:

- `max_reconnects` is a hard upper bound on reconnect attempts;
- clean provider-side termination cannot bypass the bound;
- exceptional connection failure cannot bypass the bound;
- reconnect delay remains bounded by the existing `RetryPolicy`;
- reconnect and exhaustion telemetry remains explicit;
- exhaustion yields a canonical `DISCONNECTED` acquisition envelope and terminates the invocation.

Regression coverage was added for repeated clean connection termination, clean termination with `max_reconnects=0`, and exceptional connection failure/exhaustion.

## 3. F2 — Canonical Provider Failure Mapping

The Binance adapter now maps applicable provider failures into the existing `AcquisitionEnvelope` failure semantics using the existing `ProviderError` structure.

Covered canonical states include:

- `RATE_LIMITED` for Binance HTTP 418/429 conditions;
- `UNAVAILABLE` for bounded transport/server failures;
- `DISCONNECTED` for WebSocket reconnect exhaustion;
- `INVALID` for malformed/invalid REST or WebSocket provider payloads.

The adapter retains `BinanceTransportError` as an internal/provider-specific exception mechanism, while externally returned acquisition results carry canonical acquisition state and `provider_error` where required by `CTR-P2-001`.

No new canonical state, Enum, Stable ID, contract, or identity model was introduced.

## 4. Provider-Neutral Contract Preservation

`CTR-P2-001` remains unchanged.

Failure envelopes satisfy the existing contract requirement that non-available acquisition states carry `ProviderError`, while successful `AVAILABLE` envelopes do not carry provider errors.

No Phase 3 validation/normalization was introduced.

## 5. Exact Changed Paths

Correction-cycle paths:

- `src/meylux/acquisition/binance.py`
- `tests/test_acquisition/test_binance_adapter.py`
- `docs/build-reports/BR-P2-002.md`

Existing Step 2 paths retained without unrelated modification:

- `src/meylux/acquisition/__init__.py`
- `pyproject.toml`

## 6. Implementation Commits

Correction implementation commits on the authoritative Repository include:

- `3b921444b35bfd6578bd35d41333b8f0410c9e55` — reconnect bound and canonical REST failure mapping;
- `48fae33e73a66cf3f6659ffdaba49485824ed6cf` — regression coverage for clean disconnect and canonical failure mapping;
- `f81b467c6e84dd03be4316fd61d3768188c26088` — canonical stream payload-failure mapping and invalid-payload handling.

## 7. Deterministic Test Coverage Added

`tests/test_acquisition/test_binance_adapter.py` now explicitly covers:

- clean-disconnect reconnect exhaustion;
- zero-reconnect clean termination;
- exceptional reconnect exhaustion;
- REST rate-limit mapping to `RATE_LIMITED`;
- HTTP invalid-response mapping to `INVALID`;
- malformed REST JSON mapping to `INVALID`;
- malformed WebSocket payload mapping to `INVALID`;
- canonical `ProviderError` presence and fields.

The existing provider-neutral acquisition contract regression suite remains unchanged and is part of the required validation boundary.

## 8. Required Validation Commands

The authoritative repository CI definition specifies:

```text
python -m compileall -q src config tests
python -m unittest discover -s tests -v
```

These commands are the required repository-level compilation and test boundary.

No successful GitHub Actions execution result is claimed for the correction commits. The GitHub status surface currently contains no completed run evidence.

No test count, pass count, benchmark, or CI success is fabricated.

## 9. External Binance Validation

Previously executed external transport probes remain separate from deterministic repository tests:

```text
REST_STATUS=PASS
REST_LAST_UPDATE_ID_TYPE=int
```

and:

```text
WS_STATUS=PASS
WS_EVENT=trade
```

Those probes validate public Binance transport reachability only. They are not a substitute for repository-level test execution or CONTROL verification.

## 10. VPS / Execution Environment

The existing `/srv/meylux-v2` checkout remains stale relative to the authoritative Repository. A non-destructive Git fetch was attempted but could not authenticate through the approved VPS environment without introducing an unapproved credential path.

No V1 modification, production deployment, service restart, migration, uncontrolled long-running provider runtime, trading, account, leverage, balance, transfer, withdrawal, custody, or capital operation occurred.

No credentials were added to the Repository, test suite, logs, or Build Report.

## 11. Scope Preservation

No implementation or activation was performed for:

- `STEP-P2-003` / MEXC;
- collector/persistence/replay functionality;
- Phase 2 operational hardening as a separate Step;
- Phase 3 validation/normalization;
- Phase 4 quantitative computation;
- AI/specialist intelligence;
- provider-neutral architecture redesign;
- contract redesign;
- production deployment;
- V1 mutation;
- unrelated cleanup/infrastructure work;
- trading or capital/account operations.

## 12. Producer Status

**`IMPLEMENTED / TESTED / UNVERIFIED`**

`TESTED` denotes that the deterministic regression coverage has been added and the correction implementation has been prepared for repository-level execution. It does **not** mean `VERIFIED` by CONTROL.

The Producer does not claim `TO-P2-002 VERIFIED`, `STEP-P2-002 VERIFIED`, Phase 2 completion, or authorization for `STEP-P2-003`.

## 13. Handoff

Corrected implementation and updated `BR-P2-002` are handed back to:

**`ROL-V2-001 — CONTROL / REVIEWER`**

for independent re-audit and verification under `AR-P2-AUDIT-002`.

Governed transition:

**`TO-P2-002 → IMPLEMENTED / TESTED / UNVERIFIED → CONTROL independent re-audit`**
