# BR-P4-009 — Post-Closure Security Boundary Hardening

**Task Order:** TO-P4-009  
**Project:** MEYLUX V2  
**Phase / Step:** PH-P4 / STEP-P4-006  
**Producer Role:** ROL-V2-002 — PRODUCER / ARCHITECT-BUILDER  
**Status:** IMPLEMENTED / TESTED — CONTROL AUDIT PENDING  
**Closure State:** PH-P4, STEP-P4-006 and G-4 were not modified by this implementation.

## 1. Scope and lifecycle boundary

TO-P4-009 authorized one forward database-grant hardening migration after migrations 0001–0005, together with deterministic PostgreSQL-backed evidence.

Implemented scope is limited to:

1. revoke application-role UPDATE on `meylux.raw_acquisition_events`;
2. remove UPDATE from the `meylux_admin` default table privileges for future tables;
3. narrow `meylux.canonical_event_outbox` UPDATE to `published_at` and `published_stream_id`;
4. preserve SELECT/INSERT, DELETE/TRUNCATE boundaries, canonical/quantitative semantics and existing relay SQL;
5. provide real PostgreSQL evidence.

No VPS/SentinelX application was performed by Producer. No Phase/Step/G-4 lifecycle synchronization was performed.

## 2. Required pre-implementation evidence

### 2.1 Migrations 0001–0005 and ordering

Repository evidence identified exactly these existing migrations:

1. `0001_database_foundation.sql`
2. `0002_raw_acquisition_staging.sql`
3. `0003_canonical_persistence_event_outbox.sql`
4. `0004_canonical_quality_state_alignment.sql`
5. `0005_quantitative_foundation.sql`

The new forward migration is exactly:

`0006_application_role_grant_hardening.sql`

The migration harness was updated only to invoke 0006 after 0005.

### 2.2 Existing raw-table grant model

Migration 0001 contains:

`GRANT SELECT, INSERT, UPDATE ON ALL TABLES IN SCHEMA meylux TO meylux_app;`

and:

`ALTER DEFAULT PRIVILEGES FOR ROLE meylux_admin IN SCHEMA meylux GRANT SELECT, INSERT, UPDATE ON TABLES TO meylux_app;`

Migration 0002 defines `meylux.raw_acquisition_events` and does not itself grant a narrower UPDATE boundary.

CONTROL's recorded post-closure evidence independently identified the observed raw-table UPDATE as inherited from the pre-existing `meylux_admin` default privilege.

### 2.3 Existing canonical outbox grant model

Migration 0003 defines `meylux.canonical_event_outbox` with:

- `sequence_no`
- `event_id`
- `record_id`
- `event_type`
- `event_time`
- `payload_json`
- `published_at`
- `published_stream_id`

and explicitly grants:

`GRANT SELECT, INSERT, UPDATE ON meylux.canonical_event_outbox TO meylux_app;`

Therefore the existing application-role UPDATE boundary was table-wide before TO-P4-009.

### 2.4 Actual relay SQL

The existing relay publication method in `src/meylux/persistence/canonical.py` executes:

```sql
UPDATE meylux.canonical_event_outbox
   SET published_at=now(),published_stream_id=$2
 WHERE event_id=$1 AND published_at IS NULL
```

The relay therefore requires UPDATE on exactly the two publication-state columns while using `event_id` in the predicate. `meylux_app` already retains SELECT on the outbox, so the predicate remains readable after column-level UPDATE narrowing.

### 2.5 Complete raw UPDATE search

Repository implementation/test search found no legitimate application path that executes UPDATE against `meylux.raw_acquisition_events`.

The Producer search covered:

- `src/**/*.py`
- `tests/**/*.py`, excluding the new TO-P4-009 negative-permission test itself.

No production or existing-test match was found.

The only TO-P4-009 UPDATE against the raw table is the intentional negative permission probe in the new PostgreSQL-backed security test; it is not a legitimate production path.

### 2.6 Existing privilege/relay tests

Before TO-P4-009, P3-008 persistence tests covered canonical persistence/outbox behavior using FakeConnection and FakeRedis. They did not establish PostgreSQL privilege semantics.

TO-P4-009 therefore adds a real PostgreSQL-backed test that exercises:

- table/column privilege probes;
- default-privilege inheritance before and after hardening;
- real application-role INSERT/SELECT on raw acquisition;
- real application-role denied UPDATE/DELETE/TRUNCATE on raw acquisition;
- the exact relay UPDATE as `meylux_app`;
- denied unrelated outbox UPDATE;
- denied outbox DELETE/TRUNCATE;
- migration order/integrity;
- migration idempotence.

### 2.7 Verified inventory baseline

CONTROL's authoritative post-closure evidence records:

- canonical inventory: 156;
- quantitative inventory: 3 indicator vectors / 1 market-structure event / 1 regime state.

No runtime database was mutated by Producer. These verified inventories were not reopened, rewritten, or used as test fixtures.

## 3. Migration implementation

### Exact migration identifier

`0006_application_role_grant_hardening`

### Exact migration file

`migrations/versions/0006_application_role_grant_hardening.sql`

### Migration SQL

```sql
SET TIME ZONE 'UTC';

ALTER DEFAULT PRIVILEGES FOR ROLE meylux_admin IN SCHEMA meylux
    REVOKE UPDATE ON TABLES FROM meylux_app;

REVOKE UPDATE ON meylux.raw_acquisition_events FROM meylux_app;

REVOKE UPDATE ON meylux.canonical_event_outbox FROM meylux_app;
GRANT UPDATE (published_at, published_stream_id)
    ON meylux.canonical_event_outbox TO meylux_app;

INSERT INTO meylux.schema_migrations(version)
VALUES ('0006_application_role_grant_hardening')
ON CONFLICT(version) DO NOTHING;
```

The migration is idempotent under the established repository convention:

- `REVOKE` is safe to repeat;
- `GRANT` is safe to repeat;
- the migration record uses `ON CONFLICT(version) DO NOTHING`.

### Rationale

The default-privilege correction prevents future `meylux_admin`-owned tables from silently inheriting application-role UPDATE.

The explicit raw-table revoke removes the already-inherited UPDATE from the existing raw table without touching SELECT/INSERT.

The outbox table-level UPDATE is revoked and replaced by column-level UPDATE on only the two fields used by the verified relay SQL.

No DELETE/TRUNCATE grant is introduced or removed beyond preserving the existing denied boundary.

## 4. Migration integration and integrity

Changed migration harness:

`infrastructure/postgres/migrate.sh`

The harness now executes 0006 immediately after 0005.

Migrations 0001–0005 were not edited, reordered, deleted, or rewritten.

Repository blob SHA integrity evidence for the five legacy migrations remained:

- 0001: `4cb44b7eb2cb37170793ee03605fb1cbcb587c0e`
- 0002: `27c1c05f9a8e9739d71521c326326501d29cfbed`
- 0003: `15749baff0a2848f419ab1fc80181a2941db9949`
- 0004: `2ba2cdf69f548b7df7f8386f567228cedbeaf99f`
- 0005: `fd0afb2187625d29c527d35ca856b2e7d3caaf9e`

Final 0006 content SHA:

`6019c912b5edf20a49b619423e5faa558c6a2f35`

Final migration harness content SHA:

`3c54fc741cc880ffc095aad2dc89501959c2e65c`

## 5. Real PostgreSQL evidence

The authoritative Producer-side PostgreSQL evidence was executed in GitHub Actions CI Core Run #1134:

- Run ID: `35504212725`
- Job ID: `106061149941`
- Result: SUCCESS
- Full foundation discovery: 449 tests, SUCCESS
- The TO-P4-009 test used an ephemeral `timescale/timescaledb:2.29.2-pg16` PostgreSQL instance.
- The database was built from migrations 0001–0005 before pre-state probes.
- The repository migration harness then applied 0006.
- 0006 was re-applied directly to establish idempotence.
- The test skips cleanly when Docker/PostgreSQL is unavailable.
- CI Docker Foundation explicitly skips the ephemeral test during its pre-database self-check because that workflow starts its governed database only after self-checks; CI Core is the evidence-bearing PostgreSQL execution boundary for this test.

### Actual pre-hardening command output

```text
TO-P4-009 PRE default privileges for meylux_app: ['INSERT', 'SELECT', 'UPDATE']
TO-P4-009 PRE table privileges raw/outbox: t|t|t|f|f|t
TO-P4-009 PRE future-table UPDATE inherited: t
TO-P4-009 PRE outbox column UPDATE privileges: t|t|t
```

Interpretation:

- raw SELECT = true;
- raw INSERT = true;
- raw UPDATE = true before hardening;
- raw DELETE/TRUNCATE = false;
- outbox table UPDATE = true before hardening;
- default future-table UPDATE was inherited;
- all tested outbox columns were UPDATE-capable before narrowing.

### Actual post-hardening command output

```text
TO-P4-009 POST default privileges for meylux_app: ['INSERT', 'SELECT']
TO-P4-009 POST table privileges raw/outbox: t|t|f|f|f|t|t|f|f|f
TO-P4-009 POST outbox column UPDATE privileges: t|t|f|f
TO-P4-009 POST future-table privileges: t|t|f|f|f
```

Interpretation:

- raw SELECT/INSERT retained;
- raw UPDATE removed;
- raw DELETE/TRUNCATE remain denied;
- outbox SELECT/INSERT retained;
- outbox table-wide UPDATE removed;
- outbox UPDATE retained for `published_at` and `published_stream_id`;
- outbox UPDATE denied for `event_id` and `event_type`;
- future tables inherit SELECT/INSERT but not UPDATE/DELETE/TRUNCATE.

## 6. Raw-table behavior evidence

Actual application-role evidence:

```text
TO-P4-009 APP raw SELECT after INSERT: 1
TO-P4-009 APP raw UPDATE denied rc=1: ERROR:  permission denied for table raw_acquisition_events
```

The test also executed raw DELETE and TRUNCATE as `meylux_app` and asserted both were rejected.

Therefore the required raw acquisition application boundary is preserved as SELECT/INSERT-only with DELETE/TRUNCATE still denied.

## 7. Relay compatibility evidence

The exact existing relay UPDATE was executed as `meylux_app`.

Actual command result:

```text
TO-P4-009 APP relay UPDATE rc=0: UPDATE 1
TO-P4-009 relay persisted publication state: t|stream:test
```

The relay therefore continues to update `published_at` and `published_stream_id` successfully under the narrowed privilege model.

An unrelated outbox mutation was also attempted:

```text
TO-P4-009 APP unrelated outbox UPDATE denied rc=1: ERROR:  permission denied for table canonical_event_outbox
```

This establishes that column-level narrowing is compatible with the actual relay SQL without changing application behavior.

## 8. Idempotence evidence

Actual re-application evidence:

```text
TO-P4-009 idempotent 0006 schema_migrations count: 1
```

The final migration list observed in the ephemeral database was exactly:

```text
0001_database_foundation
0002_raw_acquisition_staging
0003_canonical_persistence_event_outbox
0004_canonical_quality_state_alignment
0005_quantitative_foundation
0006_application_role_grant_hardening
```

The final Docker Foundation workflow also passed the repository migration harness and its database foundation verification:

- CI Docker Foundation Run #225
- Run ID: `35504212661`
- Result: SUCCESS
- Migration harness step: SUCCESS
- Database foundation verification step: SUCCESS

## 9. Tests and CI

Final evidence-bearing CI:

### CI Core

- Run #1134
- Run ID: `35504212725`
- Job ID: `106061149941`
- SUCCESS
- Full repository discovery: 449 tests, SUCCESS
- TO-P4-009 PostgreSQL-backed test included in that 449-test run.

### CI Docker Foundation

- Run #225
- Run ID: `35504212661`
- Job ID: `106061149607`
- SUCCESS
- Compose validation: PASS
- Foundation image build: PASS
- Foundation self-checks: PASS
- Runtime contract packaging: PASS
- Database startup/readiness: PASS
- Migration harness: PASS
- Database foundation verification: PASS

No CI workflow file was modified.

## 10. Exact changed files

1. `migrations/versions/0006_application_role_grant_hardening.sql`
2. `infrastructure/postgres/migrate.sh`
3. `tests/test_p4_009_security_grants.py`
4. `docs/registry/artifacts.yaml`
5. `docs/build-reports/BR-P4-009.md`

No other project source, contract, engine, Decimal-policy, runtime deployment, Phase 5, or closure artifact was changed by Producer.

## 11. Implementation commits / content SHAs

- Migration implementation commit: `273ea507603b9fd24085d350df29b46a876b6dfb`; final migration content includes later evidence-only comments and has content SHA `6019c912b5edf20a49b619423e5faa558c6a2f35`.
- Migration harness commit: `330a78360e08e92f2a720d50f63ddda5e19b1cba`; final harness content SHA `3c54fc741cc880ffc095aad2dc89501959c2e65c`.
- PostgreSQL evidence test final implementation commits include `dcbaa4fbe59704995244fc856c1668b7e47f41e5`; final test content SHA `8f52649acb0935b1f7e46d89bedc15b7f848b28a`.
- BR allocation commit: `f0e7700526d421ceeb081cde422a76ee8b453a36`.

## 12. Contract / architecture preservation

Confirmed:

- frozen architecture unchanged;
- canonical data semantics unchanged;
- quantitative engines unchanged;
- Decimal policy unchanged;
- no Phase 5 implementation or authorization;
- no application credential/secret modification;
- no VPS/SentinelX mutation;
- no direct canonical/quantitative data mutation;
- no DELETE/TRUNCATE boundary expansion;
- no changes to migrations 0001–0005;
- canonical verified inventory baseline 156 preserved by non-runtime Producer boundary;
- quantitative verified inventory baseline 3/1/1 preserved by non-runtime Producer boundary.

## 13. Deviations

None.

## 14. Open questions

No new Open Question was created.

`OQ-P4-006-APP-GRANTS` remains OPEN pending the CONTROL-owned independent audit, authorized SentinelX/VPS application, and post-application re-verification. Producer does not resolve this OQ.

## 15. Closure and authority statement

This Build Report records implementation and test evidence only.

Producer does NOT declare:

- PH-P4 VERIFIED/CLOSED;
- STEP-P4-006 VERIFIED/CLOSED;
- G-4 VERIFIED/CLOSED;
- OQ-P4-006-APP-GRANTS resolved.

CONTROL retains independent audit, runtime application, post-application verification, lifecycle authority, and all closure/peripheral synchronization responsibilities.
