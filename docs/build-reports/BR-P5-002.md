# BR-P5-002 — Input Snapshot Builder & Fact Availability Verification

**Build Report ID:** `BR-P5-002`  
**Phase / Step:** `PH-P5` / `STEP-P5-002`  
**Task Order:** `TO-P5-002 Rev 1.2`  
**Producer:** `ROL-V2-002` — PRODUCER / ARCHITECT-BUILDER  
**Status:** `IMPLEMENTATION CORRECTED / VALIDATED BY FRESH CI / UNVERIFIED`  
**Correction implementation commits:** `27866e45862724883316d2dfbc19226e5d5973fc`, `c6ff41d1e366fa465db59dd56469fe7a5c68c767`, `7fb01f368bf6388758be24f6fdd5a7c29d38f663`  
**Current pre-report correction head:** `45ccf37207a020bdde4b98a10394e1aa39af3478`  
**PR:** #43

## 1. Correction disposition

This report replaces the earlier Producer baseline. It explicitly incorporates the CONTROL correction findings.

Producer makes no `VERIFIED`, `COMPLETE`, or `CLOSED` claim.

## 2. Corrected implementation

Changed P5-002 boundary:

- `contracts/specialist.py` — structured EvidenceRef provenance now supports source family, record identity, event time, knowledge time, timeframe and venue while retaining legacy fields for P5-001 compatibility; Snapshot mappings are recursively copied into intrinsic read-only `MappingProxyType` containers with tuple/frozenset sequence/set normalization, eliminating dict-subclass mutation overrides.
- `src/meylux/specialists/snapshot.py` — explicit event_time and knowledge_time are mandatory for admitted records; knowledge_time must be <= as_of; complete EvidenceRef provenance is mandatory; source metadata and EvidenceRef fields are cross-checked; specialist-output-shaped input is rejected; deterministic replay/identity is preserved.
- `src/meylux/specialists/frm.py` — USR-03 is closed to exactly three lifecycle dispositions; every unavailable row requires a governed disposition; no unverified AVAILABLE_PERSISTED claim remains in the Producer baseline; S-09 and S-18 are treated as composite requirements rather than inferred availability.
- `src/meylux/specialists/__init__.py` — exposes the corrected Snapshot/FRM boundary.
- `tests/test_p5_002_snapshot_frm.py` — adds structured provenance, explicit empty-EvidenceRef rejection, nested recursive immutability/detachment, nested identity/serialization preservation, explicit specialist-output add/remove independence, and repository-level AST dependency-path evidence.
- `docs/requirements/P5_002_FACT_REQUIREMENTS_MATRIX.md` — corrected FRM evidence and disposition model.

## 3. USR-03 semantics

Exactly:

1. `AVAILABLE_PERSISTED`
2. `PRQ_DELIVERED`
3. `UNAVAILABLE_DISPOSITIONED`

The corrected Producer baseline uses `UNAVAILABLE_DISPOSITIONED` for all 18 domains because live persisted-fact availability is CONTROL-owned runtime evidence and has not been established by Producer. This avoids converting source-code capability, table existence, or historical evidence into a runtime claim.

S-09 requires all mandatory risk-relevant source facts; receiving a Snapshot is not proof that those facts exist.

S-18 requires both regime state and its independently sourced structural companion; one persisted component is not enough.

S-15 retains the roadmap-defined contract-only skipped disposition rather than inventing an upstream capability.

## 4. EvidenceRef semantics

The authoritative structured evidence fields are:

- `source_family`
- `record_id`
- `identity_hash`
- `event_time`
- `knowledge_time`
- `timeframe`
- `venue`

`source_reference` remains descriptive provenance and is not used as a substitute.

The builder never derives `knowledge_time` from `event_time`. Both are explicit and are independently validated.

## 5. Determinism and no-lookahead

The corrected builder:

- uses explicit UTC `as_of`;
- admits `knowledge_time == as_of`;
- rejects `knowledge_time > as_of`;
- preserves event time independently;
- sorts authoritative facts deterministically;
- deduplicates exact identical records;
- rejects same-identity contradictions;
- rejects ambiguous duplicate identities;
- computes deterministic Snapshot identity and serialization;
- has no wall-clock/provider/persistence-I/O dependency.

## 6. FRM / dependency dispositions

The corrected FRM references:

- `OQ-P5-002-PRQ1-FACT-AVAILABILITY`
- `OQ-P5-002-PRQ2-VENUE-ORDERFLOW`
- `OQ-P5-002-PRQ3-DERIVATIVES`
- `OQ-P5-002-PRQ4-QUALITY-EVIDENCE`

No additional Stable ID is created.

## 7. Roadmap §37

No new durable high-volume persistence owner is introduced.

Snapshot is deterministic/in-memory. FRM is version-controlled. Existing upstream persistence remains owned by its existing P2/P3/P4 boundaries. Any future durable Snapshot/Execution persistence requires a separately governed Step and a fresh growth/retention analysis.

## 8. Scope

No PRQ implementation, provider activation, Futures/Forex runtime, specialist execution engine, cross-specialist synthesis, scoring, trading/capital/custody capability, upstream redesign, VPS mutation, or lifecycle/registry synchronization was performed.

## 9. Validation evidence

Final corrected validation evidence:
- **CI Core:** run `35958812844` / run number `1245`; job `107502746126`; conclusion `success`; actual job log reports **502 tests — OK**. The increase from the prior 498-test baseline corresponds to the newly required C-03/C-04/C-05/C-08/C-09 coverage (with C-08/C-09 covered by the nested immutability test).
- **CI Docker Foundation:** run `35958812858` / run number `282`; job `107502746689`; conclusion `success`. Compose validation, image build, foundation self-checks, runtime contract packaging, database startup/readiness, migration harness, database boundary verification and cleanup all completed successfully.
- The first Core attempt on this same implementation baseline exposed two pre-existing PostgreSQL runtime failures in P4/P5-001 tests; the failed job was rerun and the final attempt passed. Those failures were: TimescaleDB extension duplicate during P4-009 bootstrap and PostgreSQL shutdown during P5-001 persistence test. Neither touched P5-002 implementation paths.
- These final successful runs validate the correction chain ending at delivery head `45ccf37207a020bdde4b98a10394e1aa39af3478`.
- Superseded `a06fde2e...` runs `35896316649` and `35896321167` are historical evidence only and are not the final corrected disposition.

This report records the current Producer correction baseline and only evidence already present in the repository. A later CONTROL audit may produce separate verification/closure records; this Build Report does not self-advance lifecycle state.

## 10. CONTROL-owned runtime boundary

Producer has no VPS authority. Live persisted-fact availability, runtime provenance, current container state and any SentinelX evidence remain CONTROL-owned.

No VPS operation was performed by Producer.

## 11. Producer lifecycle boundary

The final Producer delivery state is:

**corrected implementation + corrected FRM + corrected tests + Build Report + actual validation evidence delivered to CONTROL**

Nothing in this report advances Step lifecycle status.


## 12. CONTROL finding-by-finding correction matrix

1. **Audit determination:** the prior delivery was not accepted for verification/closure; Producer makes no verification or closure claim in this report.
2. **BR-P5-002 allocation:** `BR-P5-002` is the formally allocated Build Report identity used by this delivery; no competing Build Report identity was created.
3. **P5-001 source-publication anomaly:** no P5-001 reimplementation or corrective change was made by Producer; the independently CONTROL-fixed P5-001 publication state remains outside this Task Order.
4. **PR #43 scope:** the effective PR remains limited to the seven authorized P5-002 files; no `BR-P5-001` or `tests/test_p5_001_foundation.py` material is present.
5. **CI correction:** the superseded historical run pair is not the final evidence; fresh applicable Core and Docker validation is rerun for this correction baseline.
6. **USR-03 `AVAILABLE_PERSISTED`:** treated as runtime evidence state only; source code, table existence, contracts, roadmap text and historical P4 evidence are not substituted for live persisted availability.
7. **S-09 composite availability:** every mandatory risk-relevant source fact must be independently reconstructed with source, persisted fact, provenance, disposition and governed OQ/DD; Snapshot receipt alone is insufficient.
8. **S-18 and similar composite rows:** a composite requirement is unavailable unless every mandatory component is available and evidenced; partial component presence is not promoted to availability.
9. **OQ/DD references:** every unavailable requirement uses only the four governed IDs: `OQ-P5-002-PRQ1-FACT-AVAILABILITY`, `OQ-P5-002-PRQ2-VENUE-ORDERFLOW`, `OQ-P5-002-PRQ3-DERIVATIVES`, `OQ-P5-002-PRQ4-QUALITY-EVIDENCE`; no new IDs are invented.
10. **`knowledge_time` mandatory:** every admitted Snapshot fact requires explicit `knowledge_time`; no exception is permitted.
11. **Event/knowledge distinction:** `event_time` and `knowledge_time` remain separate explicit values; neither is copied into the other.
12. **EvidenceRef completeness:** every admitted authoritative fact requires at least one complete structured EvidenceRef containing source family, record ID, identity hash, event time, knowledge time, timeframe and venue, with cross-checks against the authoritative record.
13. **FRM source authority:** all 18 specialist rows retain row-level source audit covering schema/table, columns/identity, granularity/timeframe, actual runtime evidence boundary, owner/dependency route and governed disposition.
14. **No-lookahead:** exact `as_of`, strict future rejection, explicit knowledge time, preserved event/knowledge timestamps and deterministic replay identity remain enforced; future information cannot enter the authoritative Snapshot.
15. **Specialist independence:** Snapshot construction does not consume specialist outputs, specialist ordering cannot alter identity, Snapshot content is recursively immutable, and static dependency evidence verifies that no specialist execution/output import path was introduced.
16. **Roadmap §37:** no new durable high-volume Snapshot persistence owner, runtime retention mechanism, or upstream ownership transfer is introduced; future durable Snapshot persistence remains separately governed.
17. **Branch/main reconciliation:** PR #43 remains based on current `main` with no Producer-side deletion/reversion of P5-001 changes.
18. **Build Report completeness:** this `BR-P5-002` records the corrected baseline, changed files, FRM, Snapshot semantics, event/knowledge-time boundary, EvidenceRef contract, CI/Docker evidence, limitations and Producer lifecycle boundary.
19. **Producer lifecycle boundary:** Producer delivers implementation, tests, FRM, Build Report and validation evidence only; no `VERIFIED`, `COMPLETE` or `CLOSED` claim is made.
20. **CONTROL VPS boundary:** Producer performs no VPS operation; the CONTROL-observed `server-l6rf` runtime state and SentinelX evidence remain CONTROL-owned.
21. **Required correction output:** this correction delivery contains the corrected implementation, corrected tests, corrected FRM/requirements artifacts, Snapshot contract, EvidenceRef semantics, S-09/S-18 dispositions, governed OQ/DD references, fresh CI/Docker evidence and corrected Build Report.
22. **Continuation Duty:** Producer continued the authorized correction cycle through implementation, tests, static audit evidence, Build Report correction and fresh validation, stopping only at the natural Producer boundary requiring independent CONTROL verification.

**Additional observation — authority boundary:** the repository's upstream persistence schemas do not expose a dedicated `knowledge_time` field. This remains an evidence/ownership limitation and is not silently resolved by mapping `persisted_at` or `event_time` to `knowledge_time`.

## 13. Explicit remaining evidence boundaries

- Live row counts and live persisted availability are not Producer evidence.
- P4 persistence schemas inspected in the repository do not expose a dedicated `knowledge_time` field. Producer therefore does not map `persisted_at` to `knowledge_time`.
- A future upstream delivery must supply governed knowledge-time semantics before its facts can enter an authoritative P5 Snapshot.
- CONTROL remains the sole authority for SentinelX runtime verification and Step closure synchronization.
