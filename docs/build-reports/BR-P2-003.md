# BR-P2-003 — MEXC Acquisition Adapter

**Task Order:** `TO-P2-003`  
**Phase:** `PH-P2`  
**Step:** `STEP-P2-003`  
**Producer:** `ROL-V2-002` — PRODUCER / ARCHITECT-BUILDER  
**Status:** `VERIFIED`

## CONTROL verification disposition

CONTROL independently re-audited the corrected MEXC Spot WebSocket implementation and accepted the correction under `AR-P2-AUDIT-003`. The previous blocking F1 protocol mismatch is resolved. The corrected implementation uses the current documented Spot protobuf channel forms and binary protobuf market-data decoding, while explicitly rejecting obsolete legacy JSON market-data payloads.

Authoritative execution evidence:
- `CI Core` Run #218 / Run ID `34626462032`;
- job `repository-foundation` → `SUCCESS`;
- `python -m compileall -q src config tests` → `PASS`;
- `python -m unittest discover -s tests -v` → `PASS`;
- reported result: `Ran 104 tests in 0.720s` / `OK`;
- corrected implementation commit: `947caef206b3b37dfe13d79606caf182618f39fa`.

Live MEXC REST/WebSocket runtime probing was not available and remains explicitly UNVERIFIED. Documentation/protobuf definitions are specification evidence only.

## Scope and implementation

The MEXC public Spot acquisition adapter remains behind `CMP-P2-001` / `CTR-P2-001` and provides the authorized public REST/bootstrap and live-stream acquisition capabilities, bounded retry/reconnect, canonical failure mapping, provider-neutral provenance/identity/timestamps/sequencing/serialization, and Binance isolation.

Corrected WebSocket channels:
- trade: `spot@public.aggre.deals.v3.api.pb@100ms@<SYMBOL>`;
- incremental depth: `spot@public.aggre.depth.v3.api.pb@100ms@<SYMBOL>`;
- kline: `spot@public.kline.v3.api.pb@<SYMBOL>@Min1`.

No contract, frozen architecture, Binance behavior, Phase 3/4 scope, credentials, trading/account/capital functionality, V1 activity, production deployment, or later Phase 2 Step was introduced.

## Exact governed paths

- `src/meylux/acquisition/mexc.py`
- `tests/test_acquisition/test_mexc_adapter.py`
- `docs/build-reports/BR-P2-003.md`
- `docs/registry/phase2-artifacts.yaml`

## Final status

Producer state before CONTROL verification: `IMPLEMENTED / TESTED / UNVERIFIED`.

CONTROL final disposition: **`VERIFIED`** under `AR-P2-AUDIT-003`.

No live external-network PASS is claimed.
