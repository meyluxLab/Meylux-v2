# BR-P2-003 — MEXC Acquisition Adapter

**Task Order:** `TO-P2-003`  
**Phase:** `PH-P2`  
**Step:** `STEP-P2-003`  
**Producer:** `ROL-V2-002` — PRODUCER / ARCHITECT-BUILDER  
**Status:** `IMPLEMENTED / TESTED / UNVERIFIED`

## 1. Scope

Implemented the MEXC public Spot acquisition adapter strictly behind the existing provider-neutral acquisition boundary `CMP-P2-001` / `CTR-P2-001`.

Implemented capabilities:
- public REST exchange information / instrument bootstrap;
- public REST order-book snapshot;
- public REST recent trades;
- public REST klines;
- public WebSocket trade, incremental depth, and kline subscription/parsing;
- bounded REST retry;
- bounded WebSocket reconnect;
- canonical `AcquisitionState` / `ProviderError` mapping;
- provider-neutral provenance, provider/instrument identity, timestamps, source sequencing, status, deterministic envelope identity/serialization;
- provider isolation from Binance;
- telemetry hooks for acquisition/retry/stream lifecycle events.

No validation/normalization, persistence, replay, collector, Phase 3, Phase 4, AI, trading, account, capital, or production functionality was added.

## 2. Exact repository paths changed

- `src/meylux/acquisition/mexc.py` — new MEXC provider adapter.
- `tests/test_acquisition/test_mexc_adapter.py` — deterministic MEXC adapter and isolation tests.
- `docs/build-reports/BR-P2-003.md` — this Producer Build Report.
- `docs/registry/phase2-artifacts.yaml` — supplemental traceability entry for `BR-P2-003`.

No changes were made to:
- `contracts/acquisition.py` (`CTR-P2-001`);
- `src/meylux/acquisition/provider.py` (`CMP-P2-001`);
- Binance adapter or Binance tests;
- frozen architecture;
- Phase 3 or Phase 4 artifacts;
- V1;
- credentials/secrets/runtime production configuration.

## 3. Stable IDs

No new canonical contract, provider-neutral boundary, or competing identity Stable ID was created.

Existing governed IDs integrated/affected:
- `PH-P2`
- `STEP-P2-003`
- `TO-P2-003`
- `CMP-P2-001`
- `CTR-P2-001`
- `GOV-BOUNDARY-001`

New governed artifact:
- `BR-P2-003` — this Build Report.

## 4. Implementation details

### REST/bootstrap

MEXC public Spot V3 endpoints are isolated inside the adapter:
- `/api/v3/exchangeInfo`
- `/api/v3/depth`
- `/api/v3/trades`
- `/api/v3/klines`

Provider wire payloads remain raw/staging acquisition payloads. No Phase 3 validation or normalization is performed.

### WebSocket

The adapter uses the public MEXC WebSocket endpoint and emits provider-neutral envelopes for:
- trade;
- incremental depth;
- kline.

Subscription count is bounded to MEXC's documented 30-subscription connection limit. Subscription and stream parsing remain MEXC-specific inside the adapter.

### Failure handling

- HTTP 429 maps to `RATE_LIMITED` with bounded retry.
- HTTP 5xx maps to `DEGRADED` with bounded retry.
- other HTTP 4xx maps to `UNAVAILABLE`.
- transport failures map to `UNAVAILABLE` with bounded retry.
- malformed JSON / malformed provider payloads map to `INVALID`.
- exhausted WebSocket reconnects map to `DISCONNECTED`.
- provider API error payloads map to canonical `UNAVAILABLE` / `ProviderError`.

Retry/reconnect is bounded; there is no unbounded loop.

## 5. Deterministic local validation

Authoritative CI execution:

- Workflow: `CI Core`
- Run: `#207`
- Run ID: `34624166318`
- Job: `repository-foundation`
- Commit tested: `b21c3b7bd7ecc269935ad3d3d280740fa0a4a519`
- Job conclusion: `success`
- `Compile foundation` step: `success`
- `Run foundation tests` step: `success`

The workflow executed the repository's required commands:
```text
python -m compileall -q src config tests
python -m unittest discover -s tests -v
```

The CI job reports both command steps successful. This is actual GitHub Actions execution evidence, not a local or inferred result.

The MEXC test suite covers:
- provider identity/capability isolation;
- REST order-book mapping and sequencing;
- trades and deterministic event identity;
- kline raw-row preservation;
- exchange-info bootstrap;
- bootstrap composition;
- HTTP 429 bounded retry;
- transport failure bounded retry;
- invalid JSON;
- provider API errors;
- WebSocket trade/depth parsing;
- canonical invalid stream failure;
- bounded reconnect;
- subscription/message limits;
- canonical serialization/event identity;
- Binance isolation;
- absence of credential/signature configuration in the adapter.

## 6. External-network validation

No live MEXC REST/WebSocket probe was executed as Producer runtime evidence in this environment.

The development execution environment available to this session did not provide working outbound DNS/network connectivity for a direct MEXC request. Therefore no live connectivity result is claimed.

MEXC official API documentation was consulted for the public REST and WebSocket wire/API definitions. Documentation review is not represented as runtime execution evidence.

## 7. Credentials / secrets

No MEXC API key, secret, cookie, token, signature, private authentication material, or other credential was introduced into repository artifacts, tests, logs, or this Build Report.

The implemented Step uses public market-data endpoints only.

## 8. VPS / development activity

No VPS deployment or production/runtime activation was performed for this Step.

No V1 system was accessed or modified.

## 9. Deviations / Open Questions / Known Design Considerations

### Deviations
None identified against `TO-P2-003`.

### Open Questions
None blocking Producer completion.

### Known Design Considerations
- Live MEXC REST/WebSocket execution evidence remains outstanding because the current development environment could not establish outbound network connectivity. This is explicitly unverified rather than treated as passed.
- WebSocket parsing is implemented for the JSON public stream representation documented for the adapter boundary. Protocol-buffer transport variants are not introduced as a new dependency or separate architecture in this Step.

## 10. Explicit out-of-scope confirmation

Not implemented:
- `STEP-P2-004`;
- `STEP-P2-005`;
- `STEP-P2-006`;
- collector integration;
- raw/staging persistence;
- replay/idempotency persistence;
- Phase 3 validation/normalization;
- Phase 4 quantitative/market-structure computation;
- AI/specialist intelligence;
- production deployment;
- V1 mutation/runtime activity;
- trading, order placement/cancellation, leverage, balances, transfers, withdrawals, custody, account control, or capital operations.

## 11. Producer completion state

**`IMPLEMENTED / TESTED / UNVERIFIED`**

The implementation and CI evidence are Producer evidence only. `TO-P2-003` / `STEP-P2-003` remain subject to independent CONTROL audit and verification.

## 12. Handoff to CONTROL

Please independently verify:
1. exact changed paths and diff scope;
2. provider-neutral boundary preservation;
3. MEXC-specific isolation;
4. REST/bootstrap and WebSocket implementation;
5. canonical failure/degradation mapping;
6. bounded retry/reconnect behavior;
7. provenance/timestamp/sequence/identity/serialization semantics;
8. deterministic test evidence and CI Run `#207`;
9. absence of credentials/secrets and unauthorized scope;
10. external-network limitation as explicitly reported.

**Producer requests CONTROL independent verification of `BR-P2-003` / `TO-P2-003`.**
