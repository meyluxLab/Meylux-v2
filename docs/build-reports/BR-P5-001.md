# BR-P5-001 — Producer Build Report

**Task Order:** `TO-P5-001`  
**Phase / Step:** `PH-P5` / `STEP-P5-001`  
**Producer:** `ROL-V2-002` — PRODUCER / ARCHITECT-BUILDER  
**Status:** PRODUCER EVIDENCE DELIVERED — UNVERIFIED  
**Producer branch:** `producer/to-p5-001-foundation`  
**Final implementation commit:** `756f9a723af4cc3300239e6037f9d12c5e3bd89e`  
**Pull Request:** #42 — P5-001: Contract, Evidence Model, Config & Persistence Foundation

## 1. Implementation Summary

Implemented the authorized STEP-P5-001 foundation only:

- provider-neutral specialist contract/evidence model;
- Stage-1 Input Snapshot boundary with deterministic identity and explicit no-lookahead enforcement;
- explicit specialist lifecycle/status taxonomy;
- evidence provenance references;
- deterministic, float-free contract serialization and SHA-256 identity;
- bounded configuration validation with version, provenance, environment, units, ranges and immutable read-only safety constraints;
- append-only specialist-output persistence model;
- database-level UPDATE/DELETE rejection through the existing governed mutation trigger;
- least-privilege application/backup grants;
- duplicate/replay idempotency using deterministic identity hash;
- focused negative/boundary/config/persistence/migration tests;
- migration harness integration.

No Futures acquisition adapter, Forex implementation, specialist implementation group, cross-specialist synthesis, trading/capital/custody capability, private provider access, or upstream P2/P3/P4 ownership change was introduced.

## 2. Exact Files Changed

1. `contracts/specialist.py`
2. `src/meylux/specialists/__init__.py`
3. `src/meylux/specialists/config.py`
4. `src/meylux/specialists/persistence.py`
5. `config/specialists.yaml`
6. `migrations/versions/0007_specialist_foundation.sql`
7. `infrastructure/postgres/migrate.sh`
8. `tests/test_p5_001_foundation.py`
9. `tests/test_p4_009_security_grants.py`

The ninth file is a compatibility correction to the existing P4 security test harness: its historical migration assertions were changed to assert only the legacy 0001–0006 migration set, so the newly authorized P5 migration does not invalidate unrelated historical integrity assertions. No P4 production implementation was changed.

## 3. Contract / Evidence Semantics

### Stage-1 independence

`SpecialistRequest` exposes only:

- `specialist_id`
- authoritative `InputSnapshot`
- versioned `SpecialistConfigRef`

The snapshot contract rejects nested specialist-output/dependency fields. This makes the Stage-1 boundary enforceable at the contract boundary rather than relying solely on documentation.

### Input Snapshot

`InputSnapshot.build()` derives a deterministic SHA-256 identity from:

- snapshot version;
- `as_of`;
- canonicalized facts;
- provenance references.

Equivalent fact/provenance ordering produces the same snapshot identity. Facts whose `knowledge_time` exceeds `as_of` are rejected.

### Evidence

`EvidenceRef` requires:

- evidence identifier;
- source type;
- resolvable source reference;
- 64-character SHA-256 identity hash;
- optional UTC observation time;
- optional content version.

SUCCESS/PARTIAL specialist outputs require at least one evidence reference.

### Numeric / serialization safety

Authoritative specialist contract serialization rejects Python floats and non-finite `Decimal` values. Decimal values are serialized deterministically and UTC timestamps are normalized explicitly. No NaN/Inf fallback or fabricated numeric state is introduced.

### Status taxonomy

The implemented `SpecialistStatus` vocabulary is:

`SUCCESS`, `PARTIAL`, `INSUFFICIENT_DATA`, `UNAVAILABLE_INPUT`, `SKIPPED`, `DISABLED`, `FAILED`, `TIMEOUT`.

Upstream fact status is separately represented so missing, unavailable, invalid, stale and contradictory facts are not collapsed into success/failure ambiguity.

## 4. Configuration

`config/specialists.yaml` is intentionally JSON-compatible YAML. This provides deterministic parsing with the repository's existing standard-library-only CI environment; JSON is a valid YAML subset.

The configuration carries:

- `name`;
- `schema_version`;
- `configuration_version`;
- `environment`;
- `provenance`;
- parameter type and unit;
- explicit value;
- minimum/maximum bounds;
- immutable safety constraints.

The validator rejects attempts to enable trading, capital movement, custody or private-provider access.

Configuration identity is a deterministic SHA-256 over the canonical configuration material.

## 5. Persistence

Migration `0007_specialist_foundation.sql` creates:

`meylux.specialist_outputs`

The table persists:

- specialist identity/version;
- snapshot identity/version;
- configuration identity/version/environment;
- explicit status/reason;
- findings;
- evidence references;
- complete deterministic payload;
- identity hash;
- persistence timestamp.

Integrity controls include:

- status CHECK constraint;
- SUCCESS/PARTIAL evidence-reference requirement;
- deterministic identity UNIQUE constraint;
- existing governed append-only trigger for UPDATE/DELETE;
- application SELECT/INSERT only;
- application UPDATE/DELETE/TRUNCATE/REFERENCES/TRIGGER revoked;
- backup SELECT only.

Persistence uses `ON CONFLICT (identity_hash) DO NOTHING` for deterministic duplicate/replay behavior.

## 6. Test Inventory

Focused P5 tests cover:

- deterministic identity under reordered equivalent inputs;
- lookahead rejection;
- specialist-to-specialist dependency rejection;
- NaN/non-finite and float rejection;
- explicit missing/unavailable state;
- explicit contradictory state;
- SUCCESS evidence requirement;
- confidence boundary;
- complete status taxonomy;
- deterministic serialization;
- configuration loading;
- configuration minimum-bound rejection;
- immutable safety constraint rejection;
- duplicate/replay idempotency;
- failed output without fabricated findings;
- append-only migration and least-privilege SQL assertions.

## 7. Actual Test Execution Evidence

### Final CI Core

**Workflow run:** `35873726468`  
**Job:** `107224138549`  
**Conclusion:** `success`

The final run recorded:

- all focused `test_p5_001_foundation` tests passing;
- `Ran 464 tests in 21.806s`;
- `database migration: PASS`.

The focused P5 tests were individually reported as `ok`, including configuration, deterministic identity, lookahead, contradictory/missing state, specialist independence, serialization, persistence replay, and migration assertions.

### Final CI Docker Foundation

**Workflow run:** `35873726466`  
**Job:** `107224138282`  
**Conclusion:** `success`

The Docker foundation run executed the migration harness successfully, including:

`0007_specialist_foundation.sql`

and recorded:

`database migration: PASS`

The migration created `meylux.specialist_outputs`, its lookup index, append-only trigger and grants/revokes successfully.

## 8. Correction During Validation

The first CI Core attempt exposed a pre-existing historical P4 security-test assumption that enumerated only migrations 0001–0006. The failure was not a specialist implementation failure; it was an outdated test-harness assertion exposed by the newly authorized migration.

The correction was made by narrowing that historical assertion to the explicitly governed legacy 0001–0006 set. The final CI Core and Docker runs then completed successfully.

## 9. Architecture / Scope Preservation

Preserved:

- `DOC-V2-ARCH-001`;
- upstream P2/P3/P4 ownership;
- provider neutrality;
- read-only invariant;
- no-fabrication invariant;
- deterministic evidence model;
- Stage-1 independence;
- Futures as current product scope without implementing Futures acquisition here.

No frozen architecture, Stable ID, upstream contract, or Phase boundary was modified.

## 10. Runtime / VPS Evidence

No Producer VPS operation was performed.

No runtime/VPS claim is made.

The database evidence cited above is CI Docker Foundation execution evidence, not production/VPS evidence.

## 11. Dependencies / Deviations / Open Questions

### Concrete dependency

The known Futures acquisition capability gap remains outside this Task Order and was not implemented here. This foundation is market/contract-type neutral and preserves the existing canonical `market_type`, `contract_type`, and `contract_multiplier` distinctions.

No additional concrete upstream dependency was discovered that blocks the foundation implementation.

### Deviation

The repository's existing CI environment does not install a YAML parser. Therefore `config/specialists.yaml` uses the JSON-compatible YAML subset and the standard library parser to preserve deterministic, dependency-free configuration loading.

This does not alter the configuration contract or architectural boundary.

### Open Questions

No new project Open Question was created by the Producer.

## 12. Evidence References for CONTROL Audit

- Producer implementation commit: `756f9a723af4cc3300239e6037f9d12c5e3bd89e`
- PR: `#42`
- Final CI Core run: `35873726468`
- Final CI Core job: `107224138549`
- Final CI Docker Foundation run: `35873726466`
- Final CI Docker Foundation job: `107224138282`
- P5 focused test file: `tests/test_p5_001_foundation.py`
- Specialist contract: `contracts/specialist.py`
- Configuration: `config/specialists.yaml`
- Persistence implementation: `src/meylux/specialists/persistence.py`
- Persistence migration: `migrations/versions/0007_specialist_foundation.sql`

## 13. Producer Boundary

This Build Report does not declare:

- VERIFIED;
- ACCEPTED;
- COMPLETE at project/governance level;
- CLOSED;
- Phase closure;
- Step closure.

Registry synchronization, `CURRENT_CHECKPOINT.json`, README synchronization, Change Ledger updates, specialized registry closure and final lifecycle determination remain CONTROL-owned under ADR-GOVERNANCE-012.

COMPLETED — ALL IN-SCOPE CHANGES APPLIED AND VALIDATED
