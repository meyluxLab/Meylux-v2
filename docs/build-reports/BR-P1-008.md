# BR-P1-008 — Disaster Recovery / Restore Verification

**Project:** Meylux V2
**Phase:** `PH-P1 — Infrastructure Foundation`
**Step:** `STEP-P1-008 — Disaster Recovery / Restore Verification & Phase 1 Closure`
**Task Order:** `TO-P1-008`
**Producer:** `ROL-V2-002 — PRODUCER / ARCHITECT-BUILDER`
**Status:** `IMPLEMENTED / TESTED / EXECUTED / UNVERIFIED`

## 1. Scope

Executed only within `PH-P1 / STEP-P1-008 / TO-P1-008` against V2 host `server-l6rf` and repository `/srv/meylux-v2`.

No V1 mutation, market acquisition, provider runtime, specialist/AI functionality, trading/order execution, capital/custody/withdrawal functionality, alternate broker, or architecture replacement was introduced.

## 2. Repository State at Execution Start

- Branch: `main`
- Starting HEAD: `86b7f135f8720be26b97d6964faf426b78042a9c`
- Starting status: `main...origin/main [ahead 14, behind 32]`
- Working tree: clean

The repository copy of `TO-P1-008.md` was not present in the checked-out tree at execution start. The Producer used the direct CONTROL authorization supplied for this Step and did not reconstruct or invent a Task Order artifact.

## 3. Runtime Baseline

Actual V2 containers observed with `sudo docker ps`:

- `compose-db-1` — `timescale/timescaledb:2.29.2-pg16` — healthy
- `compose-redis-1` — `redis:7.4.6-alpine` — healthy
- `compose-api-1` — running
- `compose-collector-1` — running
- `compose-worker-quant-1` — running
- `compose-worker-ai-1` — running

Database runtime query returned PostgreSQL `16.15`; installed extensions included TimescaleDB `2.29.2` and PL/pgSQL `1.0`.

Database ports were container-only (`5432/tcp` with no host binding). Redis Compose command is `redis-server --appendonly no`, confirming Redis is not configured as an authoritative persistence/backup boundary.

## 4. Backup Evidence

A temporary, non-authoritative verification path was used: `/tmp/meylux-p1-008/meylux-p1-008.dump`.

Actual backup used the existing `meylux_backup` role and the live database container environment without exposing the password in the command or report.

Exit code: `0`.

Actual result:

- Backup size: `11526` bytes
- SHA-256: `10224e2f05156cb1ea5b4a5b0ec80ac59ee445d3f0d1c4f6e9f3ad53f53da29e`
- Custom archive created successfully.

`pg_dump` emitted a TimescaleDB circular-foreign-key warning for internal `continuous_agg`; the archive was nevertheless produced successfully and was subsequently restored with `pg_restore --exit-on-error`. No warning was suppressed or converted into a fabricated result.

## 5. Restore Evidence

An isolated target `meylux_p1_008_restore` was created separately from the authoritative database.

Restore command used `pg_restore --exit-on-error --no-owner --no-privileges`.

Result: exit code `0` and successful restore.

The authoritative `meylux` database was not overwritten or destroyed. The isolated restore database was dropped after verification.

## 6. Schema / Migration Integrity

Authoritative and restored migration state matched exactly:

`0001_database_foundation | 2026-09-09 22:27:43.893634+00`

Schema-only comparison of the `meylux` schema passed after normalizing only the generated PostgreSQL dump-session `\\restrict` token:

`SCHEMA_COMPARE=PASS`

The existing migration harness was rerun against both authoritative and restored databases using explicit `bash` invocation because the mounted script did not carry an executable bit inside the container.

Authoritative migration rerun: `database migration: PASS` — exit `0`.

Restored migration rerun: `database migration: PASS` — exit `0`.

Both reruns reported `INSERT 0 0`; no migration duplication or silent repair occurred.

## 7. Representative Data Integrity

P1 contains no market/business dataset at this boundary. The available authoritative persisted data is migration metadata.

Source:

`1|0001_database_foundation|0001_database_foundation|2026-09-09 22:27:43.893634+00|2026-09-09 22:27:43.893634+00`

Restore:

`1|0001_database_foundation|0001_database_foundation|2026-09-09 22:27:43.893634+00|2026-09-09 22:27:43.893634+00`

No missing business value was fabricated.

## 8. Database Failure / Recovery Evidence

`sudo docker restart compose-db-1` — exit `0`.

Post-restart:

- container returned to `running`;
- health returned to `healthy`;
- `pg_isready` reported accepting connections;
- migration metadata remained intact.

## 9. Queue / Worker Recovery Evidence

The live Redis Streams recovery boundary was exercised with a unique temporary stream/group namespace:

1. create temporary stream/group;
2. enqueue marker;
3. deliver to consumer A;
4. verify pending state;
5. claim by consumer B;
6. ACK;
7. verify pending count `0`;
8. remove temporary objects.

Actual result:

`REDIS_STREAM_RECOVERY=PASS`

`AFTER_ACK_PENDING=0`

`worker-quant` and `worker-ai` were restarted successfully and returned to `running`.

The deterministic queue suite covers bounded concurrency, retry transition, retry crash-window recovery, stale pending recovery, DLQ, timeout, failure isolation, retention, and ACK/backlog accounting.

## 10. Direct Project-Client Runtime Limitation

Direct execution of the project `RedisQueue` implementation inside `compose-worker-quant-1` was not converted into a PASS because the runtime image lacks the Python `redis` package:

`ModuleNotFoundError: No module named 'redis'`

No dependency/image/architecture change was introduced merely to manufacture a PASS. The actual Redis Streams protocol was independently exercised through the live Redis runtime, while project-level queue behavior remains covered by the deterministic test suite.

## 11. Redis Authority Boundary

Actual runtime configuration: `redis-server --appendonly no`.

Actual Redis persistence inspection: `aof_enabled:0`.

PostgreSQL/TimescaleDB remains the authoritative persistence boundary.

## 12. Observability Evidence

The P1 observability implementation remained intact. Queue failure/recovery tests emitted structured events including `queue.retry`, `queue.retry.recovered`, `queue.recovered_stale`, `queue.dlq`, `worker.started`, `worker.completed`, and timeout/failure paths.

Dedicated observability suite: `12/12 PASS`.

No secret value was written into this report or repository artifacts.

## 13. Cleanup Evidence

The isolated restore database and temporary backup/schema-comparison artifacts were removed after verification.

Final runtime remained:

- database healthy;
- Redis healthy;
- API running;
- collector running;
- worker-quant running;
- worker-ai running.

## 14. Test Evidence

Targeted observability suite: `12/12 PASS` — exit `0`.

Full repository suite: `86/86 PASS` — exit `0`.

`python3 -m compileall -q src tests` — PASS / exit `0`.

`git -c safe.directory=/srv/meylux-v2 diff --check` — PASS / exit `0`.

## 15. Deviations / Limitations

1. The repository did not contain a checked-out `docs/task-orders/TO-P1-008.md` at execution start; direct CONTROL authorization was used.
2. No production backup destination is frozen by P1; `/tmp/meylux-p1-008` was temporary verification storage only.
3. The mounted migration script lacked an executable bit; explicit `bash` invocation passed against both source and restored targets.
4. The worker image lacks Python `redis`; this was correctly recorded as a limitation, not a PASS.
5. `pg_dump` emitted a TimescaleDB circular-foreign-key warning for internal catalog structures; restore still succeeded with `pg_restore --exit-on-error`.

## 16. Open Questions

No new architecture, security, authority, or implementation Open Question was created by this Step.

The production backup destination remains an environment/operational configuration decision for the appropriate governed boundary; this Step does not invent or freeze one.

## 17. Prohibited-Scope Confirmation

No market-data acquisition, Binance/MEXC provider runtime activation, specialist/AI intelligence, trading/order execution, capital/custody/withdrawal/account control, V1 modification, replacement of PostgreSQL/TimescaleDB, replacement of Redis Streams, alternate broker/observability platform, later-phase functionality, or frozen architecture/Stable ID/contract/security-boundary redesign was introduced.

## 18. Final Producer State

**`IMPLEMENTED / TESTED / EXECUTED / UNVERIFIED`**

Independent CONTROL verification and the Phase 1 closure decision remain outstanding in this Producer report.