# BUILD REPORT — BR-P1-003

**Build Report ID:** `BR-P1-003`  
**Task Order:** `TO-P1-003`  
**Phase:** `PH-P1` — Infrastructure Foundation  
**Step:** `STEP-P1-003` — Database Foundation  
**Producer:** `ROL-V2-002` — PRODUCER / ARCHITECT-BUILDER  
**Architecture:** `DOC-V2-ARCH-001` — RATIFIED / FROZEN  
**Verification Authority:** CONTROL / REVIEWER — `ROL-V2-001`  
**Status:** `PRODUCED / UNVERIFIED`

## 1. Scope Executed

The authorized Database Foundation scope was implemented and the material CI/runtime validation finding identified by CONTROL was corrected without changing the ratified/frozen architecture, constitutional invariants, governed contracts, Stable IDs, interfaces, security boundaries, phase sequence, or later-step scope.

Implemented scope:

- PostgreSQL/TimescaleDB persistence foundation using the architecture-pinned TimescaleDB/PostgreSQL image.
- Initial deterministic SQL migration establishing the V2 database schema boundary and migration bookkeeping.
- TimescaleDB extension activation within the database foundation.
- `meylux_admin`, `meylux_app`, and `meylux_backup` role boundary.
- Least-privilege application access: SELECT/INSERT/UPDATE on foundation tables, with DELETE/TRUNCATE/REFERENCES/TRIGGER explicitly revoked; migration metadata is additionally protected from application INSERT/UPDATE.
- Backup role read-only access to the foundation schema.
- Environment-provided role passwords through the migration harness; no credentials stored in repository artifacts.
- Database timezone fixed to UTC through migration configuration.
- Idempotent migration bookkeeping through `meylux.schema_migrations`.
- Deterministic, side-effect-free Python database configuration model; no database I/O at import time.
- Docker Compose wiring required to execute the database foundation inside the existing V2 database container without introducing public database ports.
- CI runtime validation with explicit Compose interpolation environment on database `exec` steps.
- Migration harness correction required for runtime execution: psql variable interpolation for role-password SQL is supplied through stdin rather than `psql -c`, preserving safe literal quoting semantics.
- Automated CI validation for Compose parsing, image build, database startup, migration execution, extension presence, migration record, role state, privilege boundary, and UTC timezone.

## 2. Changed / Created Artifacts

| Path | Action | Stable ID | Purpose |
|---|---|---|---|
| `migrations/versions/0001_database_foundation.sql` | CREATE / REVISE | — | Initial authoritative database foundation migration. |
| `infrastructure/postgres/migrate.sh` | CREATE / REVISE | — | Migration execution harness and deterministic database configuration application; runtime password interpolation correction. |
| `config/database.py` | CREATE | — | Pure database configuration boundary. |
| `infrastructure/compose/docker-compose.yml` | REVISE | — | Mount migration foundation and supply environment-driven database role inputs. |
| `.env.example` | REVISE | — | Non-secret database foundation configuration template. |
| `tests/test_database_foundation.py` | CREATE / REVISE | — | Structural and configuration self-tests for the database foundation. |
| `.github/workflows/ci-docker.yml` | REVISE | — | CI execution and runtime database foundation verification, including explicit runtime environment propagation. |
| `docs/build-reports/BR-P1-003.md` | CREATE / REVISE | `BR-P1-003` | Formal Producer Build Report and final execution evidence. |

No new implementation Stable IDs were invented.

## 3. CONTROL Finding and Corrective Action

CONTROL identified a material execution finding in Docker Foundation CI Run `34204620235`: the runtime `docker compose exec` commands did not receive the required Compose interpolation variables, causing `Wait for database` to fail and preventing migration and boundary verification.

Corrective actions were limited to the authorized Step 3 execution path:

1. Added the existing CI database environment values to the runtime validation steps so Compose interpolation succeeds for `docker compose exec`.
2. Passed the same database variables explicitly into the database container for the migration harness, because host-side CI environment variables are not automatically inherited by `docker compose exec` into the target container.
3. The corrected runtime execution exposed an existing migration-harness issue: psql `-v` SQL-literal interpolation using `:'app_password'` / `:'backup_password'` inside `psql -c` was not interpolated in that invocation form. The harness was corrected to feed those SQL statements through stdin while retaining psql variable literal quoting.

These corrections remain strictly within the Database Foundation / Step 3 boundary.

## 4. Database Foundation

The foundation establishes:

- TimescaleDB extension.
- `meylux` schema owned by `meylux_admin`.
- `meylux.schema_migrations` bookkeeping table.
- `meylux_admin` as the database foundation administration/ownership role through the Compose bootstrap boundary.
- `meylux_app` as the bounded application role.
- `meylux_backup` as the bounded backup/read role.

The migration is idempotent for extension, schema, role creation, and migration-record insertion.

## 5. Privilege Boundary

`meylux_app` receives SELECT/INSERT/UPDATE privileges on foundation tables, but cannot DELETE or TRUNCATE them. On `meylux.schema_migrations`, application INSERT and UPDATE are explicitly revoked so migration state remains controlled by the administrative boundary.

`meylux_backup` receives SELECT-only access to the foundation schema tables.

Role passwords are supplied only through environment variables consumed by the migration harness. No password value is persisted in repository artifacts.

## 6. Deterministic Database Configuration

The migration executes with UTC session time and applies UTC as the database timezone configuration.

`config/database.py` exposes only explicit configuration values and performs no filesystem, network, wall-clock, or database access at import time.

The migration harness uses `set -euo pipefail` and PostgreSQL `ON_ERROR_STOP=1` so migration failures are surfaced rather than silently continued.

## 7. Producer Self-Tests and Final Execution Evidence

### Repository / CI Core

`CI Core` completed successfully for the corrected implementation state.

- Latest observed `CI Core` run: `34206106250` — **SUCCESS**.
- Python compilation and repository unit-test suite: **PASS**.
- Database foundation unit tests: **PASS**; 11 tests executed in the Docker Foundation workflow.

### Docker / Database Foundation CI

Final corrected runtime validation:

- Workflow: `CI Docker Foundation`
- Run ID: `34206106269`
- Job: `docker-foundation`
- Corrected implementation commit: `14c583ae3289bf6343f80b115a0f920a7b69166b`
- Final workflow merge ref executed by GitHub Actions: `7ebf21d5977519e3459d1bdeefb3a1fee0641ca5`
- Job conclusion: **SUCCESS**

Completed evidence:

- Compose configuration validation: **PASS**
- Foundation image build: **PASS**
- Foundation self-checks: **PASS**
- Database container startup: **PASS**
- Database readiness: **PASS** (`pg_isready` reached `accepting connections`)
- Database migration harness: **PASS** (`database migration: PASS`)
- TimescaleDB extension verification: **PASS** (`timescaledb` present)
- Migration record verification: **PASS** (`0001_database_foundation` present)
- Role verification: **PASS** — `meylux_admin`, `meylux_app`, and `meylux_backup` all present with login enabled for the CI execution.
- Application migration-metadata privilege verification: **PASS** — SELECT=`true`, INSERT=`false`, UPDATE=`false`, DELETE=`false`.
- Database timezone verification: **PASS** (`UTC`)
- Cleanup: **PASS**

The successful runtime evidence satisfies the previously blocked execution boundary for this Step, subject to independent CONTROL verification.

## 8. Scope / Governance Check

The implementation and corrective execution work did not:

- modify the ratified/frozen architecture;
- modify constitutional invariants;
- activate `STEP-P1-004` or later P1 steps;
- implement Phase 2+ functionality;
- activate or mutate V1/V1 VPS/runtime;
- perform production deployment;
- perform provider or market runtime activity;
- perform trading, capital control, or autonomous execution;
- modify governed contracts or existing Stable IDs;
- modify `CURRENT_CHECKPOINT.json`.

The database foundation remains infrastructure-only and contains no market/provider, trading, AI, or downstream data-contract functionality.

## 9. Deviations / Execution Notes

**Authorized corrective implementation adjustment:** The migration harness was corrected after the first successful runtime environment propagation exposed the psql `-c` variable-interpolation issue described in Section 3. This was strictly necessary to satisfy the authorized Database Foundation runtime execution boundary.

**Execution-environment limitation:** The Producer environment does not provide a local PostgreSQL/TimescaleDB Docker runtime, so database engine execution was delegated to the repository's GitHub Actions environment.

No other deviation from the authorized Step 3 scope was identified.

## 10. Open Questions / Known Design Considerations

No blocking Open Question remains for the authorized Database Foundation implementation based on the completed Producer execution evidence.

The database role/password model intentionally keeps secret material outside repository artifacts. Physical production secret provisioning and environment execution remain outside this Step and require the governed Operator path when authorized.

## 11. Producer Disposition

**IMPLEMENTED / SELF-TESTED / EXECUTION EVIDENCE PRODUCED**

The authorized implementation and required corrective execution work for `TO-P1-003` have been completed and subjected to the applicable repository and Docker/database self-tests.

This Build Report intentionally does **not** claim `VERIFIED`, `APPROVED`, or `COMPLETE / VERIFIED`.

Independent CONTROL audit remains the governing verification step.
