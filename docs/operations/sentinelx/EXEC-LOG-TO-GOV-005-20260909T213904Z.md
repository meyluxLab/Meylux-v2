# MEYLUX V2 — EXEC-LOG — TO-GOV-005

**Execution ID:** `exec-gov-005-20260909T213904Z`
**Task ID:** `TO-GOV-005`
**Phase/Step:** `NONE / NONE`
**Executor Role:** `ROL-V2-001` — CONTROL / REVIEWER
**Target:** dedicated Meylux V2 VPS `server-l6rf`; Host ID `host_3774c70bc3624713`
**Start Time UTC:** `2026-09-09T21:38:59Z`
**Authorization:** `TO-GOV-005` / `ADR-GOVERNANCE-010` / `ADR-GOVERNANCE-011` / `AR-GOV-004` / Project Owner direction
**Repository Version Context:** `meyluxLab/Meylux-v2` / `main` / deployed commit `258e3cccea3fd024294e5c86d56ff070a6cd1cb8`

## Execution Boundary

This execution establishes the physical V2 VPS foundation only. It does not reopen `STEP-P1-001`, `STEP-P1-002`, or `STEP-P1-003`, does not implement `STEP-P1-004`, does not authorize later Steps, does not touch V1, and does not perform market/provider/trading/capital activity.

## Sections A–E

### A — Target / Host Baseline — EXECUTED / PASS

- Target: `server-l6rf`, Host ID `host_3774c70bc3624713`.
- OS: Ubuntu 24.04 LTS (Noble), x86_64, kernel `6.8.0-35-generic`.
- CPU/RAM: 4 vCPU / 15 GiB RAM; swap 0.
- Root filesystem: 76 G total, 71 G available, 3% used; inode usage 1%.
- Time synchronization: `systemd-timesyncd` active.
- Baseline listeners: SSH TCP/22 plus local resolver sockets; no Meylux application listener.
- `/opt`: SentinelX only; no V1 `/opt/meylux` checkout. `/srv` was initially empty.
- Git `2.43.0`; Python `3.12.3`; Docker absent before installation.
- SentinelX `0.11.18`, target identity and service state verified.
- V1 isolation confirmed; no V1 host was accessed or modified.

### B — OS Prerequisites — EXECUTED / PASS

- `apt update` PASS; Noble repositories reachable.
- No blanket upgrade performed despite 205 upgradeable packages being reported.
- No conflicting Docker packages installed before Docker setup.
- `curl` present; time sync active; resource headroom sufficient.
- No swap creation, firewall modification, or unrelated cleanup performed.

### C — Git / Repository State — EXECUTED / PASS

- Official repository: `meyluxLab/Meylux-v2`; private repository; default branch `main`.
- Current `main` at execution time: `258e3cccea3fd024294e5c86d56ff070a6cd1cb8`.
- Interactive SSH `gh` authentication was completed for `meyluxLab` with HTTPS Git protocol and required repository scope.
- SentinelX initially saw a different `gh` environment; `sudo gh auth status --hostname github.com` confirmed the authenticated root GitHub CLI context without exposing credentials.
- `/srv/meylux-v2` was used as the execution-local V2 checkout path. This is an operational implementation choice because `/opt/meylux` is reserved for frozen V1 and the repository does not define a V2 checkout path.
- Clone succeeded through authenticated `gh repo clone`.
- Remote: `https://github.com/meyluxLab/Meylux-v2.git`.
- Branch: `main`; `HEAD == origin/main == 258e3cccea3fd024294e5c86d56ff070a6cd1cb8`.
- Working tree clean at the recorded consistency check.

### D — Python / Runtime Prerequisites — EXECUTED / PASS

- Authoritative project Python contract: `>=3.12,<3.14`.
- Host Python `3.12.3` satisfies the contract.
- Dockerfile installs the application into the container; no host application dependency installation was performed.

### E — Docker Engine / Compose — EXECUTED / PASS

- Docker Engine `29.8.0`; Compose plugin `v5.5.1`; containerd `2.3.5`; Buildx `0.37.0`.
- Docker daemon active.
- `hello-world` daemon/container path test PASS; temporary image removed afterward.
- Docker system storage returned zero images/containers/volumes/build cache after the temporary test, before V2 deployment.
- Official V2 Compose network/resource state was subsequently created only by the repository Compose definition.

## Section F — Configuration / Secret Provisioning — EXECUTED / PASS

Authoritative `.env.example` defines the local Docker/Database Foundation inputs: `MEYLUX_DB_NAME`, `MEYLUX_DB_USER`, `MEYLUX_DB_PASSWORD`, `MEYLUX_APP_PASSWORD`, and `MEYLUX_BACKUP_PASSWORD`. The repository explicitly excludes secrets from committed artifacts.

For this V2 foundation execution, an execution-local `.env` was created from that project-defined template with cryptographically random local-only password values generated on the VPS. Values were never printed or recorded. The file is root-owned with mode `600` and remains outside repository history. This is foundation/local runtime material, not a production secret claim. No provider or market credentials were provisioned.

The known Environment Manifest wording discrepancy concerning physical execution authority was not silently rewritten.

## Section G — Compose Validation — EXECUTED / PASS

- Repository Compose SHA: `bee600b5ce7de90967568ed094d55ca573e07b05`.
- `docker compose ... config --quiet` PASS with the execution-local environment.
- Defined services verified: `db`, `redis`, `api`, `collector`, `worker-quant`, `worker-ai`.
- Defined network: `meylux-net`.
- Defined volume: `meylux-db-data`.
- DB/Redis have no published host ports in the authoritative Compose definition.

## Section H — Image Pull / Build — EXECUTED / PASS

- Pulled authoritative foundation images: `timescale/timescaledb:2.29.2-pg16` and `redis:7.4.6-alpine`.
- Built the four application images from the repository Dockerfile using `python:3.12.11-slim`.
- Actual resulting image digests were recorded from Docker: TimescaleDB `sha256:289d55704b1b3ee8263cd3805c6930f9cd54506835a8f19f9b85dad17d5c5a8f`, Redis `sha256:3b73847e72874be07e6657b129a94761662b79bc0f679273757d4218573b2a98`; application build manifest digests were also captured in the execution output.
- Build completed successfully.

## Section I — Database + Redis Startup — EXECUTED / PASS

- `db` started and reached Compose health `healthy`.
- `redis` started and reached Compose health `healthy`.
- Redis readiness returned `PONG`.
- No restart loop observed.

## Section J — Database Foundation Execution — EXECUTED / PASS

The repository migration harness `infrastructure/postgres/migrate.sh` and authoritative migration `migrations/versions/0001_database_foundation.sql` were inspected before execution.

- Initial direct host invocation failed with `psql: command not found` (return code 127); diagnosis: the harness requires the PostgreSQL client and the authoritative runtime places that client inside the DB container.
- No repository change was made for this failure.
- The same authoritative harness was then executed inside the running `db` container with the container-provided environment variables mapped to the harness inputs; result `database migration: PASS`.
- Verified PostgreSQL `16.15`.
- Verified TimescaleDB `2.29.2`.
- Verified schema `meylux`.
- Verified migration record `0001_database_foundation`.
- Verified roles `meylux_admin`, `meylux_app`, `meylux_backup`; application and backup roles have login enabled after harness password application.
- Verified application privilege boundary: schema USAGE and migration-table SELECT true; migration-table INSERT/UPDATE/DELETE false.
- Verified database timezone `UTC`.
- The DB startup log contained a TimescaleDB informational version notice and initialization background-worker messages; PostgreSQL reached ready state and subsequent checks passed.

## Section K — Application Runtime Startup — EXECUTED / PASS WITH IMPLEMENTATION BOUNDARY

- Started `api`, `collector`, `worker-quant`, and `worker-ai` using authoritative Compose.
- All four containers remained running; restart count was `0` at verification snapshot.
- Repository runtime entrypoint is explicitly a minimal foundation process and does not yet implement API, collection, worker, market, or database functionality. Therefore no unsupported claim of application-level DB query behavior, API endpoint behavior, market/provider activity, or trading behavior is made.
- From the API container, internal Docker DNS resolved `db` and `redis`, and TCP connectivity to `db:5432` and `redis:6379` passed.

## Section L — Network / Security Verification — EXECUTED / PASS

- Docker `ps` showed no published host ports for API/collector/workers.
- DB and Redis showed container-only ports (`5432/tcp`, `6379/tcp`) and no published host port.
- UFW service state was `inactive`; no firewall change was made because no authoritative requirement in this Task Order required one.
- SentinelX policy/privilege boundary remained governed; no credential values were exposed in execution evidence.
- `.env` is root-owned mode `600`.

## Section M — Persistence / Restart Verification — EXECUTED / PASS

- Named volume `meylux-db-data` was created by authoritative Compose.
- DB was controlled-restarted.
- DB returned to health `healthy`.
- After restart, migration record remained `0001_database_foundation`, timezone remained `UTC`, and TimescaleDB remained `2.29.2`.
- No volume recreation or destructive persistence shortcut was used.

## Section N — Runtime Health / Observability — EXECUTED / PASS WITH KNOWN NON-BLOCKING OBSERVATIONS

- All six Compose services running at the consolidated health snapshot.
- DB and Redis healthy; application containers running with restart count `0`.
- Resource snapshot showed low CPU and memory usage: application containers approximately 8.4 MiB each; DB approximately 115.8 MiB; Redis approximately 3.2 MiB at observation.
- Docker storage/image inventory and container state were captured.
- Redis emitted a startup warning recommending `vm.overcommit_memory=1`; no sysctl change was made because this execution did not authorize speculative host tuning. This is recorded as a Known Design Consideration, not treated as a failure.

## Section O — Repository ↔ VPS Consistency — EXECUTED / PASS

- Deployed Git commit: `258e3cccea3fd024294e5c86d56ff070a6cd1cb8`.
- `HEAD == origin/main`; working tree clean.
- Compose file SHA-256: `2a95f6d1a6c90ff70be96a2fbdb58858c8149ee591c3129a6cd0e3b95149d3d5`.
- `.env` is execution-local and not committed; permission `600`, owner `root:root`.
- Compose project points to `/srv/meylux-v2/infrastructure/compose/docker-compose.yml` and execution-local `.env`.
- No `/opt/meylux` V1 dependency was introduced.
- No provider, market, trading, capital, withdrawal, or order activity occurred.

## Section P — Execution Evidence — COMPLETE / PENDING SEPARATE VERIFICATION

**Actions:** Sections A–O executed under `TO-GOV-005` on the dedicated V2 VPS.

**Failures:**
1. SentinelX non-sudo `gh auth status` initially saw no authentication; resolved by using the authenticated root GitHub CLI context already established by the authorized operator session.
2. Direct host migration-harness invocation returned exit code 127 because `psql` is not installed on the host; resolved by executing the unchanged authoritative harness inside the DB container where `psql` is provided.

**Retries:** both conditions were genuine operational/environment issues; no fabricated failures were introduced.

**Recovery classes:** R0–R4 not exercised as genuine failure conditions requiring recovery escalation.

**Final execution result:** `EXECUTED — FOUNDATION DEPLOYMENT EVIDENCE CAPTURED; SEPARATE CONTROL VERIFICATION REQUIRED`.

**Verification status:** not yet recorded in this execution log at this boundary.

**Secret handling:** no secret values recorded.

## Section Q — Separate CONTROL Verification

`PENDING — must be recorded only after independent verification of the completed execution evidence.`

## Section R — Governance State Update

`PENDING — no lifecycle/checkpoint promotion is made by this execution log alone.`

**End Time UTC:** `2026-09-09T22:31:00Z`
