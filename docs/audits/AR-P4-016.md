# AR-P4-016 — CONTROL Independent Audit & Runtime Verification — TO-P4-009

**Task Order:** TO-P4-009  
**Build Report:** BR-P4-009  
**Project:** MEYLUX V2  
**Role:** ROL-V2-001 — CONTROL / REVIEWER  
**Phase / Step:** PH-P4 / STEP-P4-006  
**Status:** APPROVED / VERIFIED  
**Lifecycle:** POST-CLOSURE HARDENING — PH-P4 / STEP-P4-006 / G-4 closure preserved

## 1. Audit Authority and Boundary

CONTROL independently audited the Producer implementation and evidence for TO-P4-009, then applied the authorized hardening through SentinelX and performed post-application runtime verification.

This audit does not reopen PH-P4, STEP-P4-006, or G-4. No Phase 5 authorization is created.

## 2. Producer Evidence Audit

BR-P4-009 is internally consistent with TO-P4-009 and establishes:

- exactly one forward migration: `0006_application_role_grant_hardening.sql`;
- migrations `0001`–`0005` unchanged according to the recorded repository blob-SHA integrity evidence;
- `meylux_app` UPDATE removed from `raw_acquisition_events` while SELECT/INSERT remain;
- `meylux_admin` future-table UPDATE inheritance removed;
- table-wide outbox UPDATE removed and column-level UPDATE retained only for `published_at` and `published_stream_id`;
- actual relay SQL tested under the application role;
- unrelated outbox UPDATE and DELETE/TRUNCATE boundaries rejected;
- no legitimate production UPDATE path against `raw_acquisition_events` identified;
- migration idempotence established;
- CI Core Run #1134 / Run ID `35504212725` SUCCESS with 449 tests;
- CI Docker Foundation Run #225 / Run ID `35504212661` SUCCESS;
- no Producer SentinelX/VPS mutation.

**Audit result:** PASS.

## 3. Repository Implementation Audit

CONTROL verified the authoritative repository contains the TO-P4-009 migration, migration-harness integration, PostgreSQL-backed test, Task Order, and Build Report. The implementation matches the authorized privilege boundary and contains no second hardening migration.

The repository-side implementation is therefore accepted as the sole TO-P4-009 implementation baseline.

## 4. SentinelX Runtime Application

Before application, the governed VPS database reported migrations `0001` through `0005`; `0006_application_role_grant_hardening` was absent.

The deployed migration mount also lacked the new repository migration file and its mounted `migrate.sh` invoked only `0001`–`0005`. CONTROL therefore did not execute the stale deployed harness. Instead, CONTROL applied the exact authorized `0006` SQL directly through SentinelX.

Application committed successfully. Re-application of the same SQL was then performed as an idempotence check and committed with `INSERT 0 0` for the existing migration record.

No prune, service replacement, image/volume/network modification, or unrelated schema/data mutation was performed.

## 5. Runtime Grant Verification

Post-application authoritative database read-back:

| Boundary | Result |
|---|---|
| `raw_acquisition_events` UPDATE | DENIED |
| `raw_acquisition_events` SELECT | ALLOWED |
| `raw_acquisition_events` INSERT | ALLOWED |
| `canonical_event_outbox` table UPDATE | DENIED |
| `published_at` UPDATE | ALLOWED |
| `published_stream_id` UPDATE | ALLOWED |
| `event_id` UPDATE | DENIED |
| `event_type` UPDATE | DENIED |
| `0006` migration record count | 1 |
| migration order | `0001` → `0006` |

A transaction-local future-table probe created under `meylux_admin` showed SELECT/INSERT allowed and UPDATE/DELETE denied. The probe transaction was rolled back.

## 6. Actual Application-Role Behavior

Using `SET ROLE meylux_app` in transaction-scoped probes:

- `raw_acquisition_events` UPDATE was rejected by PostgreSQL permission enforcement;
- unrelated `canonical_event_outbox` UPDATE was rejected;
- `canonical_event_outbox` DELETE was rejected;
- the exact relay publication UPDATE against a temporary transaction-local outbox probe row returned `UPDATE 1` and the transaction was rolled back;
- no governed test data was left mutated by these probes.

This independently confirms that the narrowed outbox privilege is compatible with the actual relay SQL.

## 7. Runtime / G-4 Preservation Verification

Post-application SentinelX read-back:

- exactly six governed services are present: `api`, `worker-quant`, `collector`, `worker-ai`, `db`, `redis`;
- `db` and `redis` are healthy;
- database connection count for `meylux` = `4`;
- canonical candles = `156`;
- calculated indicator vectors = `3`;
- market structure events = `1`;
- market regime states = `1`;
- quantitative inventory = `3/1/1`;
- 30-minute error-pattern scan across `api`, `worker-quant`, `collector`, and `worker-ai` is clean.

No observed evidence invalidates G-4. PH-P4 and STEP-P4-006 remain CLOSED / VERIFIED.

## 8. Findings and Disposition

### Finding F-01 — TO-P4-009 implementation
**Disposition:** ACCEPTED / VERIFIED.
The Producer implementation conforms to the authorized Task Order.

### Finding F-02 — Runtime grant boundary
**Disposition:** ACCEPTED / VERIFIED.
The governed database now enforces the Owner-authorized hardening.

### Finding F-03 — Relay compatibility
**Disposition:** ACCEPTED / VERIFIED.
The exact relay UPDATE succeeds with only the two authorized column UPDATE privileges.

### Finding F-04 — Runtime-source synchronization observation
**Disposition:** RECORDED — NON-BLOCKING FOR THIS CLOSURE.
The deployed `/srv/meylux-v2` migration mount predates the repository TO-P4-009 implementation. CONTROL intentionally did not perform unapproved source/deployment synchronization; it applied the authorized SQL directly through SentinelX. The database state is verified. This observation does not invalidate the current hardening evidence or G-4 closure.

## 9. Open Question Resolution

`OQ-P4-006-APP-GRANTS` is **RESOLVED**.

Resolution: the Owner-authorized bounded hardening has been implemented, independently audited, applied through SentinelX, and runtime-verified. `meylux_app` no longer has UPDATE on `raw_acquisition_events`; future `meylux_admin` tables no longer inherit UPDATE for `meylux_app`; and `canonical_event_outbox` UPDATE is limited to `published_at` and `published_stream_id`, with the actual relay behavior preserved.

The remaining open questions are unrelated to TO-P4-009 and remain untouched.

## 10. Closure Decision

TO-P4-009 is **APPROVED / VERIFIED / COMPLETE** at the CONTROL level.

PH-P4 remains `CLOSED / VERIFIED`.

STEP-P4-006 remains `COMPLETE / VERIFIED`.

G-4 remains `ESTABLISHED / VERIFIED`.

Phase 5 remains **NOT AUTHORIZED**.

No frozen architecture, engine, Decimal policy, canonical/quantitative semantics, or Phase-4 closure criterion was changed.

CONTROL owns and has completed the required post-closure synchronization.

**CONTROL conclusion:** TO-P4-009 is fully closed on evidence. No further Producer action is authorized for TO-P4-009.