# AR-P3-008 — CONTROL Independent Audit — Authoritative Persistence, Event Handoff & G-3 Verification

**Audit SID:** `AR-P3-008`
**Phase:** `PH-P3`
**Step:** `STEP-P3-008`
**Task Order:** `TO-P3-008 Revision 1.1`
**Producer:** `ROL-V2-002` — PRODUCER / ARCHITECT-BUILDER
**Auditor:** `ROL-V2-001` — CONTROL / REVIEWER
**Decision:** APPROVED / VERIFIED
**Step disposition:** COMPLETE / VERIFIED

## 1. Independent Re-Audit Result

CONTROL independently verified the corrected repository state after the prior F1/F2 rejection.

F1 is corrected in the inspected implementation: `CanonicalPersistence.persist()` contains one quality-log INSERT execution under the assessment gate, preserves the transaction boundary, and the regression test executes the persistence path and asserts exactly one quality-log execution and one transaction entry.

F2 is corrected in the Build Report: `src/meylux/persistence/event_handoff.py` is the identified relay path and `canonical_relay.py` has no occurrence in the Build Report.

F3 is corrected: exactly one `BR-P3-008` registry record exists at `docs/build-reports/BR-P3-008.md`.

Fresh CI evidence is real and successful. CONTROL independently retrieved CI Core Run #587 / Run ID `35358800648`, job `repository-foundation`, and CI Docker Foundation Run #120 / Run ID `35358800779`, job `docker-foundation`; both concluded success.

## 2. Production Execution

CONTROL merged PR #25 into `main` as merge commit:

`9f8c1ad0bf9f74ce03ece9dbb3d5e9d8f6483b5e`.

CONTROL synchronized the authorized VPS repository at `/srv/meylux-v2` to that exact `main` commit using SentinelX.

CONTROL executed the database migration harness through the governed Docker runtime. The migration completed with `database migration: PASS`.

The observed migration state became `0001`, `0002`, and `0003`, and the canonical trade and event-outbox relations were present.

CONTROL rebuilt and recreated the API, collector, worker-quant and worker-ai containers from the synchronized repository.

## 3. Blocking Runtime Finding

The controlled real-data vertical-slice execution was attempted using the deployed image and Producer-provided runner:

`src/meylux/runtime/p3_008_vertical_slice.py`

The execution failed before pipeline execution with:

`ModuleNotFoundError: No module named 'contracts'`

The deployed Dockerfile copies `src` and installs the Python package, while the runner imports the repository-level `contracts.*` package. The runtime image therefore does not contain the required importable `contracts` package.

This is a material deployment/runtime defect. It prevents valid real-data vertical-slice evidence and consequently blocks persistence read-back, event receipt, runtime failure/recovery verification and G-3.

CONTROL did not silently rewrite the Producer implementation.

## 4. Runtime State

After deployment, the database was observed with migrations `0001`–`0003`. The relations `meylux.canonical_trades` and `meylux.canonical_event_outbox` were present.

The raw acquisition table contained zero rows. Therefore no governed AVAILABLE raw record was available for a real-data vertical slice at this execution boundary. No fallback data was fabricated.

The six Compose services were observed running after the authorized rebuild. Failed transient one-shot API containers created during runner attempts were removed as execution cleanup.

No secret value was recorded.

## 5. Disposition

The repository correction gate is accepted, but the Step cannot be marked CONTROL VERIFIED.

Current state:

`STEP-P3-008 = ACTIVE / CORRECTION REQUIRED`

Required Producer correction:

1. Correct the runtime packaging/deployment boundary so the authorized vertical-slice runner can import all of its declared runtime dependencies, including the repository `contracts` package, without unrelated scope expansion.
2. Add or update automated Docker/runtime coverage for this packaging invariant.
3. Produce fresh CI evidence after the correction.
4. Update `BR-P3-008` with the actual correction and CI evidence.
5. Return the corrected boundary to CONTROL for independent re-audit.

Producer must not modify Production EXEC-LOG, G-3 status, Checkpoint closure state, or CONTROL verification state.

The existing Production migration is not G-3 evidence.

## 6. Evidence References

- merged main: `9f8c1ad0bf9f74ce03ece9dbb3d5e9d8f6483b5e`
- Producer correction commit: `99495e9f52c0b4f8e1442f17d1392b3694cfd060`
- Build Report blob: `d747aa7b1c2d771b295ceb1864aba3ac32e9418f`
- CI Core: Run #587 / `35358800648`
- CI Docker: Run #120 / `35358800779`
- VPS: `server-l6rf` / `host_3774c70bc3624713`
- VPS repository: `/srv/meylux-v2`
- Production execution record: `docs/operations/sentinelx/EXEC-LOG-TO-P3-008-20260918T150102Z.md`

## 7. Final CONTROL Decision

**REVISE / CORRECTION REQUIRED.**

Repository correction evidence is accepted, but the deployed runtime packaging defect blocks the remaining P3-008 acceptance criteria. G-3 and closure are not reached.


## 8. Runtime Packaging Correction Re-Audit — 2026-09-18

CONTROL independently inspected Producer PR #26 and its exact three-file delta against the prior P3-008 Production synchronization boundary.

Verified repository evidence:
- infrastructure/docker/Dockerfile now copies the repository-level contracts/ tree into /app.
- The runtime image sets PYTHONPATH=/app:/app/src.
- .github/workflows/ci-docker.yml contains a Docker-image import invariant that imports the required contracts.* modules and meylux.runtime.p3_008_vertical_slice from the built image.
- contracts/ is not excluded by .dockerignore.
- docs/build-reports/BR-P3-008.md records the exact runtime correction commits and explicitly does not claim Production success or G-3.
- PR #26 changes exactly the three paths claimed by Producer.

The correction is therefore accepted as an implementation-level correction to the previously observed packaging defect.

CI verification is NOT yet evidenced. CONTROL queried the workflow runs associated with PR head 1ff0dc64bde9056053cc3a4beddc92f26401085; the available repository evidence returned no associated pull-request workflow runs. Consequently CONTROL does not accept the Producer's Docker runtime invariant as CI-tested evidence yet, and does not infer success from the code inspection.

The Step remains:

STEP-P3-008 = ACTIVE / CORRECTION REQUIRED

Next required evidence boundary:
1. obtain a fresh GitHub Actions Docker Foundation run for the corrected PR state;
2. independently inspect the completed job result and logs;
3. if successful, continue to CONTROL Production synchronization/rebuild and retry the authorized vertical slice;
4. retain the existing Production execution record as historical evidence; do not overwrite the prior failed runtime result.

No G-3, persistence read-back, event receipt, recovery verification, Step closure, or Phase 3 closure is claimed.


## 9. Fresh CI Acceptance and Production Retry Re-Audit — 2026-09-18

CONTROL independently verified the Producer's Fresh CI handoff for PR #26 at head `1ff0dc64bde9056053cc3a4beddc92f26401085c`.

Verified CI evidence:
- CI Docker Foundation Run #124 / Run ID `35365833834` / job `105667972494`: completed / success.
- The Docker job's `Verify runtime contracts packaging` step completed successfully.
- The Docker job built the foundation image, ran the packaging invariant, completed the migration harness, and completed database-boundary verification successfully.
- CI Core Run #594 / Run ID `35365834192` / job `105667972898`: completed / success.
- The repository-foundation job completed compile, P3-007 quality tests, P3-008 persistence/event tests, and the full foundation suite successfully; the retrieved log reports `Ran 273 tests ... OK`.

The CI evidence gap is therefore closed for the exact corrected PR head.

CONTROL then merged PR #26 as merge commit:

`2f61be905259c7dc5ce5649b19931a83cc9591c2`

CONTROL synchronized `/srv/meylux-v2` on `server-l6rf` to that exact commit using SentinelX. The API, collector, worker-quant and worker-ai images were rebuilt and recreated. The migration harness completed with `database migration: PASS`, and migrations `0001`, `0002`, and `0003` were present.

CONTROL retried the authorized vertical-slice runner from the rebuilt API image. The runtime packaging/import boundary no longer produced the prior `ModuleNotFoundError`. Instead, the runner reached its governed data-availability guard and returned exit code `2` with:

`vertical slice: no governed AVAILABLE raw record was found; no fallback data was fabricated`

An independent database query confirmed zero rows in `meylux.raw_acquisition_events` at that boundary. Recent collector logs showed only foundation service startup and no acquisition event. No synthetic or fabricated market data was inserted.

The current Production retry therefore does not establish canonical persistence read-back, event receipt, failure/recovery verification, or G-3. It is evidence that the prior packaging defect has been passed, but the controlled real-data execution boundary remains unavailable because the governed P2 raw/staging dataset is empty.

The official execution record is:
`docs/operations/sentinelx/EXEC-LOG-TO-P3-008-PROD-RETRY-20260918.md`

## 10. Current CONTROL Decision

`REVISE / CORRECTION REQUIRED`

The implementation/runtime packaging correction and Fresh CI evidence are accepted. Production deployment/rebuild and migration are successful. However, acceptance criteria 15, 16, 17, 22, and 23 cannot be established from the current evidence boundary, and closure synchronization cannot truthfully declare the Step or Phase 3 verified/closed.

A further Producer code correction is not presently evidenced as necessary for the observed data-availability boundary. Before closure can proceed, CONTROL requires a governed AVAILABLE raw/staging record produced by the authorized acquisition boundary, followed by a successful controlled vertical slice and the remaining independent persistence/event/recovery/G-3 verification.

`STEP-P3-008 = ACTIVE / CORRECTION REQUIRED`
`PH-P3 = ACTIVE / AUTHORIZED`
`TO-P3-008 = AUTHORIZED TO EXECUTE`


## 11. Producer Build Report Evidence Synchronization Re-Audit — 2026-09-18

CONTROL independently reviewed PR #27 before acceptance. The PR contained exactly one changed path:

`docs/build-reports/BR-P3-008.md`

The corrected Build Report was retrieved from the Producer branch at commit `01046f6b6b7e81df9a52871851d076a93e25f86e` using its real blob and was inspected as a complete artifact.

The synchronization is accepted as accurate:
- CI Core #594 / Run ID `35365834192` and CI Docker Foundation #124 / Run ID `35365833834` are identified as the final accepted Fresh CI evidence for the runtime-packaging correction state.
- The corrected PR head `1ff0dc64bde9056053cc3a4beddc92f26401085c` is recorded.
- Older #586/#119 evidence is explicitly retained as historical rather than final evidence.
- The prior `contracts` packaging defect is explicitly distinguished from the current Production data-availability boundary.
- The CONTROL-observed Production retry result is recorded as exit code 2 with zero rows in `meylux.raw_acquisition_events` and no fabricated fallback.
- The Build Report remains `PRODUCED / TESTED / UNVERIFIED`, Verification `UNVERIFIED`, Closure `OPEN`.
- No Producer-owned closure artifact was altered.

PR #27 was merged by CONTROL as merge commit:

`846aa2a1a5189dda178e9706788f1c02c174a710`

A workflow run associated with the Producer documentation commit was independently observed as CI Core Run #601, Run ID `35369757908`, completed successfully. This run does not substitute for the previously accepted implementation/runtime CI evidence and does not alter the Production verification boundary.

Current disposition remains:

`STEP-P3-008 = ACTIVE / CORRECTION REQUIRED`

The remaining blocker is not an unreviewed Build Report defect. It is the absence of governed AVAILABLE raw acquisition evidence required to execute the controlled real-data vertical slice and establish persistence read-back, event receipt, recovery evidence and G-3.

## 12. Operational Reassessment of the Prior Zero-Row Condition — 2026-09-18

CONTROL did not treat the prior `meylux.raw_acquisition_events = 0 rows` observation as a final blocker. Under TO-P3-008 and ADR-GOVERNANCE-013 Rule 1, CONTROL independently diagnosed the acquisition/runtime boundary and operated the existing authorized acquisition path as far as reachable.

The reassessment established:

- the running `collector` container is currently a foundation service entrypoint (`python -m meylux.runtime.service`) and does not instantiate the Phase 2 acquisition adapters or persist acquisition events;
- collector logs contained only the foundation startup message, so no acquisition attempt had occurred through that running service at the prior zero-row boundary;
- the existing repository Binance acquisition adapter is operationally reachable from the deployed API image;
- an actual `BinanceAdapter.fetch_trades("BTCUSDT", limit=1)` probe returned `AVAILABLE`;
- the public Binance Futures endpoint was independently reachable from the deployed runtime with HTTP 200, but this is not treated as governed acquisition evidence because the current repository contains no governed Binance Futures adapter/path;
- CONTROL then exercised the existing governed Binance Spot adapter and `RawStagingRepository` and successfully inserted one actual `AVAILABLE / TRADE` raw record;
- raw event ID: `f12b8c638b3a2e742c88bad720ca2f6b6a5f19df4f38ab3983cdcb251fbc7d7a`;
- source sequence: `6692896984`;
- independent raw read-back succeeded;
- no synthetic, fabricated or fallback market data was used.

This proves that the previous empty-table condition was operationally remediable within the existing CONTROL boundary for the existing governed Binance Spot acquisition path. It was therefore not, by itself, an A/B/C stopping condition.

## 13. Newly Established Producer Runtime Defect

After the governed AVAILABLE raw record was established, CONTROL re-executed the authorized runner:

`python -m meylux.runtime.p3_008_vertical_slice`

The runner passed the prior zero-row guard and failed during raw-row envelope reconstruction with the actual exception:

`TypeError: payload must be a mapping`

The failure occurs in `envelope_from_row()`, where `row["payload_json"]` is passed directly to `AcquisitionEnvelope`. The persisted database column is `jsonb`, while the runtime value supplied by the driver is not a mapping at this boundary. The acquisition contract correctly rejects that value.

This is a newly evidenced Producer-owned implementation/runtime defect, distinct from:
1. the already-corrected Docker `contracts` packaging defect; and
2. the prior zero-row operational condition.

CONTROL must not silently rewrite this Producer-owned implementation.

### Required Correction

ROL-V2-002 must correct the runtime row-to-envelope boundary so the existing PostgreSQL `jsonb` representation is decoded into the mapping required by `AcquisitionEnvelope` before construction, while preserving malformed/non-mapping rejection and the existing persistence schema.

Required evidence:
1. deterministic regression coverage for the row-to-envelope boundary;
2. applicable P3-008 tests and full regression;
3. fresh Docker/runtime CI evidence;
4. updated `BR-P3-008` evidence;
5. forward-ready handoff to CONTROL.

No new Task Order or architecture change is required for this correction. Producer must not modify Production, EXEC-LOG, Checkpoint closure state, G-3 state or CONTROL verification state.

## 14. Current Disposition After Operational Reassessment

The current classification is:

`STEP-P3-008 = ACTIVE / CORRECTION REQUIRED`

The stopping condition is **C — natural Producer correction boundary requiring independent CONTROL action after correction**, not A and not B.

The operational cycle has genuinely progressed beyond the previous zero-row boundary. Once the Producer correction is independently re-audited, CONTROL must continue the already-authorized runtime path rather than re-opening the prior documentation correction cycle.

G-3 remains NOT ESTABLISHED. Canonical persistence read-back, event receipt, failure/recovery verification and closure synchronization remain pending.


## 15. Corrected JSONB Re-Execution — Subsequent Runtime Boundary — 2026-09-18

CONTROL independently re-executed the authorized vertical slice after the Producer JSONB correction and fresh CI acceptance.

Before the runner execution, CONTROL synchronized `/srv/meylux-v2` to the current repository state, rebuilt and force-recreated the governed Compose services, and re-executed migrations `0001`, `0002`, and `0003`. The migration command completed with `database migration: PASS`, and database/Redis health checks were successful.

The first invocation without an entrypoint override did not execute the requested runner because the Docker image declares the foundation service as its ENTRYPOINT. CONTROL identified this from the Dockerfile and container logs and did not treat that invocation as vertical-slice evidence.

CONTROL then executed the runner with an explicit Python entrypoint. The previously observed JSONB failure:

`TypeError: payload must be a mapping`

was not reproduced. The corrected row-to-envelope boundary therefore progressed further.

A new failure was then observed:

`AttributeError: 'ValidationResult' object has no attribute 'result'`

The failure occurs at the quality-signal construction expression `outcome.result.value` in `src/meylux/runtime/p3_008_vertical_slice.py`. The `validation_outcome(...)` result is already a `ValidationResult` and does not expose a nested `result` attribute.

This is a new Producer-owned runtime implementation defect. It is not an Owner decision, not an authoritative conflict, and not a reason to alter the architecture. CONTROL must not silently rewrite the Producer implementation.

No canonical persistence, canonical read-back, event receipt, runtime recovery verification, or G-3 evidence was established by this attempt. No synthetic, fabricated, or fallback market data was used.

The official execution record is:
`docs/operations/sentinelx/EXEC-LOG-TO-P3-008-VERTICAL-SLICE-RETRY-20260918.md`

### Current disposition

`STEP-P3-008 = ACTIVE / CORRECTION REQUIRED`
`PH-P3 = ACTIVE / AUTHORIZED`
`TO-P3-008 = AUTHORIZED TO EXECUTE`
`G-3 = NOT ESTABLISHED`

The stopping condition is **C — natural Producer correction boundary requiring independent CONTROL verification after correction**.

The previously accepted JSONB correction remains accepted at implementation/CI level; this new defect requires a new bounded Producer correction cycle. No architecture or Task Order change is required.


## 16. Independent Runtime-Contract Reconciliation — 2026-09-18

CONTROL performed the requested reconciliation against the actual deployed execution context rather than relying on either Producer conclusion.

### 16.1 Exact repository state

The VPS repository at `/srv/meylux-v2` was observed at:

`9e39d1032a8dc63fe49217ad25ef0fdc430b375d`

The checked-out worktree reported `main...origin/main` with no local modifications. The exact runner source at that commit has blob SHA:

`9c445e1b398527cebd4ce4ecb4c8482cab8c5114`

The authoritative `contracts/canonical/foundation.py` blob is:

`6dca1046950a2c2e29005aaa73fb7d435d976c3c`

### 16.2 Exact deployed runtime context

The failing one-shot vertical-slice container was inspected directly through SentinelX. Its image ID was:

`sha256:366dfc4f6f49ba658b23e37ce6d407d162b49f3ef1da7bd6039d47511c159fef`

Its command was:

`python -m meylux.runtime.p3_008_vertical_slice`

The container was created at `2026-09-18T17:53:57.541072027Z` after the CONTROL rebuild/recreate boundary. The deployed `/app` source contains the same relevant implementation expression observed in the repository:

`outcome=validation_outcome(issues) if issues else normalized.result`

and:

`signals=QualitySignals(validation_status=Decimal("1.00") if outcome.result.value=="valid" else Decimal("0.00"))`

The deployed contract implementation was also inspected directly. `validation_outcome(...)` returns `ValidationOutcome(...)`, whose `result` field is a `ValidationResult`; however, that contract is only applicable when `validation_outcome(...)` is actually called.

### 16.3 Exact cause reconciliation

The observed exception is fully explained by the authoritative runner's own branch logic.

When `issues` is empty, the runner does **not** call `validation_outcome(...)`. It assigns:

`outcome = normalized.result`

`NormalizationOutcome.result` is itself a `ValidationResult` enum, not a `ValidationOutcome` wrapper. The subsequent expression:

`outcome.result.value`

therefore attempts to access `.result` on a `ValidationResult`, producing exactly:

`AttributeError: 'ValidationResult' object has no attribute 'result'`

This is not a stale-image discrepancy and not an authoritative contract conflict. The deployed runner and deployed contract are mutually consistent with the repository state, and the observed exception follows deterministically from the consumer's mixed use of the two existing result contracts.

### 16.4 Reconciliation disposition

Classification:

**IMPLEMENTATION DEFECT — CONFIRMED**

Not:
- deployment/version-context discrepancy;
- stale image/container;
- authoritative contract conflict;
- Owner decision / external authorization blocker.

The Producer's second review correctly identified the contract distinction, but the final determination is made here from independently inspected repository and deployed runtime evidence. The first proposed adapter correction must therefore **not** be accepted as-is merely because CI can pass it; the correction must address the actual mixed-contract consumer path.

No architecture change or new Task Order is required. The authoritative contract remains unchanged.

### 16.5 Current state

`STEP-P3-008 = ACTIVE / CORRECTION REQUIRED`
`PH-P3 = ACTIVE / AUTHORIZED`
`TO-P3-008 = AUTHORIZED TO EXECUTE`
`G-3 = NOT ESTABLISHED`

Stopping condition remains **C — natural Producer correction boundary**.

CONTROL will not declare G-3, persistence/read-back, event receipt, recovery verification, Step closure or Phase closure from the current evidence.


## 17. Canonical Quality-State Persistence Boundary — 2026-09-18

CONTROL independently re-executed the authorized P3-008 vertical slice after merging Producer PR #31 as 737e2b557cd1db0f8ed3ab5e228d51ff0aeb3032 and synchronizing/rebuilding the VPS runtime.

The runner progressed beyond the previously corrected validation-outcome boundary and reported structural=valid, temporal=valid, normalized=valid, quality=VALID, canonical_eligible=True for the established governed raw event f12b8c638b3a2e742c88bad720ca2f6b6a5f19df4f38ab3983cdcb251fbc7d7a.

Canonical persistence then failed before insertion with asyncpg.exceptions.CheckViolationError on canonical_trades quality-state check constraint canonical_trades_quality_state_check.

The failing row carried quality_state = VALID. Independent repository inspection confirms the authoritative DataQualityState.VALID.value is VALID, while migration 0003_canonical_persistence_event_outbox.sql defines the canonical quality-state check as quality_state = 'valid'. CanonicalPersistence.persist() passes record.quality_state.value directly to PostgreSQL. This is therefore an implementation/schema boundary mismatch, not a stale deployment and not an authoritative contract conflict.

No canonical row was inserted by this failed attempt. Event handoff, failure/recovery runtime verification and G-3 remain unestablished. No synthetic/fabricated/fallback data was used.

### Required Producer Correction

ROL-V2-002 must correct the quality-state persistence boundary so the authoritative DataQualityState contract and canonical SQL constraint agree, without weakening the VALID-only canonical gate or changing the frozen architecture. The correction must include deterministic regression coverage at the persistence/schema boundary, applicable P3-008/full regression, fresh Docker/runtime CI, and an updated Build Report. Producer must not modify Production EXEC-LOG, AR-P3-008, Checkpoint closure state, Change Ledger closure state, registries, G-3, or Phase-3 lifecycle state.

### Current Disposition

STEP-P3-008 = ACTIVE / CORRECTION REQUIRED
PH-P3 = ACTIVE / AUTHORIZED
TO-P3-008 = AUTHORIZED TO EXECUTE
G-3 = NOT ESTABLISHED

The stopping condition is C — natural Producer correction boundary requiring independent CONTROL verification after correction.

Official execution record: docs/operations/sentinelx/EXEC-LOG-TO-P3-008-VERTICAL-SLICE-RETRY-20260918-VALIDATION-FOLLOWUP.md.

## 18. Final CONTROL Correction, Runtime Verification and G-3 — 2026-09-18

The Project Owner explicitly authorized CONTROL to take direct end-to-end control of the remainder of STEP-P3-008, eliminating routine Producer correction round-trips while preserving architecture, evidence and SentinelX governance boundaries.

CONTROL therefore corrected the confirmed canonical quality-state schema mismatch directly. The authoritative contract is DataQualityState.VALID.value = "VALID"; migration 0003 constrained canonical quality state to lowercase valid. CONTROL added migration 0004 to align all five canonical quality-state constraints to exact VALID and updated the migration harness to apply it after 0003. This did not alter the authoritative contract or weaken the VALID-only canonical gate.

### 18.1 Independent automated verification

- Dedicated P3-008 suite: 24 tests — PASS.
- Full repository regression: 285 tests — PASS.
- CI Core Run #645 / Run ID 35383756721 / repository-foundation job 105725748357 / tested commit fb17a034805abd219bad8fccc757fefc8faca27e: SUCCESS.
- No separate Docker Foundation CI run was triggered by the current push, and CONTROL makes no false claim of one. Actual Docker build/recreate, migration and runtime evidence was obtained on the authorized VPS.

### 18.2 Runtime verification

CONTROL synchronized /srv/meylux-v2 on server-l6rf to fb17a034805abd219bad8fccc757fefc8faca27e, rebuilt/recreated the governed services and applied migrations 0001–0004 successfully.

The authorized vertical slice processed the real governed raw event f12b8c638b3a2e742c88bad720ca2f6b6a5f19df4f38ab3983cdcb251fbc7d7a with structural=valid, temporal=valid, normalized=valid, quality=VALID and canonical_eligible=True.

Canonical persistence succeeded with record ID 80349ae7db22c1b15a18020b9219ed8eaa7be80334e1444fb632463558e3a9a3, event ID 3b143c10856995e0519ae2080da17d5669627fe74d0e288af1b6d5d1daf122fe, and outbox sequence 1.

Independent PostgreSQL read-back confirmed the canonical row, VALID quality state, source record and lineage references. The quality log exists exactly once for the record.

The event was published to stream:canonical:market_events, Redis stream ID 1789758121669-0, and the outbox publication acknowledgement was recorded. Redis idempotency state was independently observed.

A second controlled replay returned inserted=False and published=0, confirming deterministic duplicate/replay behavior.

DB, Redis and governed application services were restarted via SentinelX. The canonical row and event publication state survived restart and services returned healthy/running.

P3-008 integration tests explicitly cover transport failure isolation; the P3-007 VERIFIED predecessor supplies the active quarantine/DLQ boundary. No rejected/unavailable evidence entered canonical truth.

### 18.3 Security and integrity verification

meylux_app has INSERT/SELECT access to canonical trades and no UPDATE/DELETE access. The canonical append-only trigger is enabled. Migration authority remains separated from application DML. No secrets were recorded. No trading/capital/custody/leverage functionality was introduced.

### 18.4 G-3 determination

CONTROL independently determines that the controlled P3-008 end-to-end evidence boundary is established:

P2 staging → Validation → Normalization → Quality → Canonical persistence → Canonical event stream → P4-ready data

The required actual evidence includes real-data execution, validation/normalization/quality pass, canonical persistence, independent read-back, normalized event receipt, deterministic replay, restart/recovery, lineage/provenance, quarantine/DLQ predecessor verification, observability and security boundaries.

The architecture performance target >5,000 normalization events/sec/core was not measured and is explicitly unverified. A second-symbol production witness was not available because only one governed AVAILABLE raw event existed; CONTROL did not fabricate another witness. Neither item is misrepresented as achieved.

G-3 = ESTABLISHED for the authorized controlled slice.

### 18.5 Final audit determination

All applicable TO-P3-008 acceptance criteria are now supported by actual repository, CI, database, Redis and SentinelX runtime evidence. Criteria not measured or not available from the governed real-data boundary are explicitly marked rather than fabricated.

AR-P3-008 = APPROVED / VERIFIED
STEP-P3-008 = COMPLETE / VERIFIED
TO-P3-008 = VERIFIED / COMPLETE
BR-P3-008 = VERIFIED
PH-P3 = READY FOR CLOSURE SYNCHRONIZATION

Official final execution record: docs/operations/sentinelx/EXEC-LOG-TO-P3-008-CONTROL-CLOSURE-20260918.md.