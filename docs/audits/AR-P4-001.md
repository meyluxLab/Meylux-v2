# AR-P4-001 — CONTROL Independent Audit — Quantitative Foundation, Contracts, Numeric Policy & Golden Vector Freeze

**Audit SID:** `AR-P4-001`
**Phase:** `PH-P4`
**Step:** `STEP-P4-001`
**Task Order:** `TO-P4-001 Revision 1.0`
**Producer:** `ROL-V2-002` — PRODUCER / ARCHITECT-BUILDER
**Auditor:** `ROL-V2-001` — CONTROL / REVIEWER
**Decision:** REVISE / CORRECTION REQUIRED
**Step disposition:** ACTIVE / CORRECTION REQUIRED

## 1. Audit Purpose

CONTROL independently audited the Producer hand-off for `TO-P4-001` against the authoritative repository state, `DOC-V2-ARCH-001`, `PH-P4`, `TO-P4-001`, `ADR-GOVERNANCE-012`, and `ADR-GOVERNANCE-013`.

The audit covered scope compliance, architecture preservation, quantitative contracts, numeric policy, pure mathematical primitives, golden-vector controls, persistence foundation, automated test evidence, CI evidence, and evidence/lifecycle integrity.

The Producer implementation is substantial and remains within the authorized P4-001 boundary. The observed precision defect was corrected in the implementation rather than weakening exact golden comparison. However, independent acceptance cannot be granted because material evidence-quality and coverage gaps remain.

## 2. Repository / PR Boundary Audited

Producer branch:
`producer/p4-001-quantitative-foundation`

Final Producer branch head submitted:
`32edc7f617be8bd84bc18fd2352cc225ee0d1696`

Producer-stated authoritative base:
`460859f30c549a2137538e1ace2c08563f1c8fa7`

CONTROL independently compared the base and head. The exact 23-commit delta contains only the P4-001 implementation/evidence paths claimed by the Producer, including quantitative contracts, numeric policy, primitives, golden infrastructure/vectors, migration foundation, tests, CI workflow path update, and BR/registry records. No later P4 engine, trading, V1, AI, or unrelated implementation was found in that delta.

Open PR:
`#32` — `feat(p4-001): establish quantitative foundation`

## 3. Architecture and Scope Audit

**Result: PASS at implementation boundary.**

The inspected delta does not modify `DOC-V2-ARCH-001`, existing P3 canonical contract files, V1, or later P4 runtime engines.

The implementation matches the authorized foundation boundary:
- five quantitative domain contract wrappers;
- shared immutable calculation result/context semantics;
- Decimal numeric boundary and deterministic serialization;
- reusable pure mathematical primitives;
- explicit insufficient-history semantics;
- exact golden-vector runner and controlled references;
- five authorized persistence-model foundations;
- relevant automated tests and CI support.

No implementation of STEP-P4-002 or later was identified.

No synthetic/fabricated market data, trading/capital functionality, or VPS/production operation was introduced.

## 4. Quantitative Contract Audit

**Result: PASS with evidence limitations.**

CONTROL inspected `contracts/quantitative/base.py` and the five domain wrappers.

Verified:
- immutable dataclasses;
- explicit CalculationStatus;
- finite Decimal-or-absence result semantics;
- non-null numeric values restricted to VALID status;
- mandatory non-empty reason;
- UTC-aware timestamp validation;
- domain-specific thin wrappers for Indicator, Market Structure, Volume Profile, Order Flow and Regime;
- no mutation of existing P3 canonical contracts.

No architecture conflict was found.

## 5. Numeric Policy Audit

**Result: PASS.**

CONTROL inspected `docs/quantitative/NUMERIC_POLICY_P4_001.md`, `src/meylux/quantitative/numeric.py`, and `src/meylux/quantitative/primitives.py`.

The implementation consistently uses Decimal at the P4-001 quantitative boundary, rejects float/bool and non-finite values, performs no implicit rounding, uses explicit ROUND_HALF_EVEN quantization, serializes in plain decimal notation, and uses exact golden comparison without epsilon tolerance.

The standard-deviation implementation places mean, variance and square-root inside an explicit high-precision Decimal context. This directly addresses the earlier observed precision defect and preserves exact comparison semantics.

The policy also correctly distinguishes safe classification of invalid mathematical inputs from explicit TypeError/ValueError for contract violations.

## 6. Mathematical Primitive Audit

**Result: IMPLEMENTATION PASS; coverage correction required.**

The eight implemented public primitives were independently identified:
- average;
- weighted_average;
- rolling_mean;
- exponential_smoothing;
- standard_deviation;
- percentile;
- accumulate;
- normalize_min_max.

The implementation is pure with respect to network, database, filesystem, wall-clock and mutable-global dependencies.

The existing unit tests cover the principal nominal and boundary behaviors claimed in the Build Report. However, the controlled golden-vector system does not currently cover all eight implemented public primitives.

The golden runner/test mapping and vector file currently cover:
- average;
- weighted_average;
- standard_deviation;
- percentile;
- exponential_smoothing.

They do not currently have controlled golden vectors for:
- rolling_mean;
- accumulate;
- normalize_min_max.

Because TO-P4-001 §4.5 requires initial vectors exercising the primitives implemented in this Step, and §6 requires golden-vector evidence, this is a material coverage gap rather than a cosmetic enhancement.

## 7. Invalid / Missing / Malformed Input Evidence

**Result: CORRECTION REQUIRED.**

TO-P4-001 §5 and §6 require coverage of missing, malformed and non-finite input behavior.

The implementation correctly rejects malformed/type-invalid numeric inputs through explicit TypeError/ValueError and rejects non-finite values. Empty history is covered as insufficient history.

However, the inspected `tests/test_quantitative_foundation.py` does not contain explicit malformed/missing-value cases such as a non-numeric object, `None`, or an invalid decimal string at the public primitive boundary.

The implementation behavior may be correct, but the required evidence is incomplete.

Required correction: add deterministic tests covering representative malformed/type-invalid and missing-value inputs for the public numeric boundary, while preserving the documented distinction between contract exceptions and CalculationResult INVALID_INPUT/INSUFFICIENT_HISTORY semantics.

## 8. Persistence Foundation Audit

**Result: IMPLEMENTATION PASS; execution-evidence correction required.**

CONTROL inspected `migrations/versions/0005_quantitative_foundation.sql` and the migration harness.

Verified from repository implementation:
- all five authorized quantitative output-family tables are created;
- context/result/version fields required by the Task Order are present;
- deterministic identity hashes are unique;
- volume-profile session ordering is constrained;
- append-only mutation rejection is attached to all five tables;
- application role receives SELECT/INSERT and explicit UPDATE/DELETE/TRUNCATE/REFERENCES/TRIGGER revocation;
- backup role receives SELECT;
- migration 0005 is registered idempotently and ordered after 0004.

However, the fresh Docker evidence only directly asserts application privileges for `calculated_indicator_vectors`. The Build Report describes the privilege boundary as verified for the five-table set, but the cited runtime query does not independently demonstrate the same privilege matrix for the other four tables.

Required correction: add a deterministic runtime database verification that checks the intended SELECT/INSERT/UPDATE/DELETE privilege matrix for all five quantitative tables, and record that exact evidence in the revised Build Report.

This finding is an evidence-coverage gap; the SQL implementation itself is consistent with the claimed privilege policy.

## 9. Golden-Reference Governance Audit

**Result: PASS for governance behavior.**

The golden runner resolves only explicitly registered function names, canonicalizes results deterministically, and compares exact values/status/reason without epsilon tolerance or dynamic expected-value mutation.

The Producer's reported earlier standard-deviation precision failure was handled by correcting the implementation and retaining the governed exact reference rather than weakening the comparison.

CONTROL therefore accepts the correction method as consistent with ADR-GOVERNANCE-013 Rule 5.

The missing three primitive vectors remain a required correction under Section 6 above.

## 10. CI Evidence Audit

**Result: PASS for execution, FAIL for Build Report traceability.**

CONTROL independently retrieved:
- CI Core Run #689 / Run ID `35396071136` / repository-foundation job `105765074416`: completed / success.
- CI Docker Foundation Run #162 / Run ID `35396071138` / docker-foundation job `105765075118`: completed / success.

Both runs are associated with the submitted final branch head `32edc7f617be8bd84bc18fd2352cc225ee0d1696`.

The workflow jobs completed their claimed compile/test and Docker/database foundation stages successfully.

However, the current `BR-P4-001.md` records the earlier intermediate evidence boundary:
- commit `f11820a5e3f75b960355e5cba360aa2ea9aa1592`;
- CI Core #687 / Run ID `35395889630`;
- CI Docker Foundation #160 / Run ID `35395889696`.

Those runs are real, but they are not the final CI runs associated with the final Producer branch head submitted for audit. The Build Report therefore does not accurately identify the final evidence boundary.

Required correction: revise `BR-P4-001.md` so the final CI evidence references the exact final branch head `32edc7f617be8bd84bc18fd2352cc225ee0d1696` and Runs #689 / #162, while retaining earlier #687 / #160 evidence only as historical context if desired.

## 11. ADR-GOVERNANCE-012 Peripheral Synchronization Audit

**Result: CLOSURE PENDING; CONTROL OWNERSHIP PRESERVED.**

The Producer correctly did not perform Step/Phase closure synchronization.

The current Build Report does not individually report the five ADR-GOVERNANCE-012 peripheral checklist items. Because the Step is not yet at closure, this does not by itself prove a Producer closure violation; however, the checklist must be explicitly completed at the eventual Step-closure evidence boundary.

CONTROL also identifies a CONTROL-owned Task Order issuance gap: `TO-P4-001` does not reproduce the mandatory five-item peripheral synchronization checklist in its Required Output section, although ADR-GOVERNANCE-012 and the Artifact Protocol require every future Step/Phase-closing Task Order to include it.

This is a governance/documentation defect in the CONTROL-issued Task Order, not a Producer implementation defect. CONTROL must correct this before Step closure and must perform the complete synchronization cycle itself.

The five checks are:
1. README vs CURRENT_CHECKPOINT;
2. artifacts.yaml exercised/created/verified artifact statuses;
3. all applicable specialized registries;
4. standalone status-bearing documents;
5. supplemental/staging registries.

The Change Ledger and CURRENT_CHECKPOINT synchronization required by the closure process remain CONTROL-owned and are not delegated to Producer.

## 12. R1–R5 Compliance Assessment

**R1 — PASS.** Producer continued through implementation, correction, regression and final hand-off to the natural independent-audit boundary.

**R2 — PASS.** Producer supplied a complete same-response hand-off message.

**R3 — PASS.** Producer reports use of complete Blob retrieval for large authoritative artifacts; CONTROL independently inspected the relevant final repository artifacts.

**R4 — PASS.** No VPS operation was performed or claimed for P4-001.

**R5 — PASS in engineering response; not yet sufficient for acceptance.** The precision defect was diagnosed from real CI evidence and corrected in the implementation. The remaining evidence/coverage gaps must be corrected before CONTROL can conclude that the maximum-quality standard has been met for the complete Step evidence boundary.

## 13. Required Producer Correction

ROL-V2-002 shall remain within TO-P4-001 and correct only the following bounded items:

1. Add controlled golden vectors and exact runner coverage for `rolling_mean`, `accumulate`, and `normalize_min_max`.
2. Add explicit malformed/missing input regression coverage required by TO-P4-001.
3. Add direct database privilege verification for all five quantitative tables.
4. Update `BR-P4-001.md` to record the exact final implementation/evidence boundary at commit `32edc7f617be8bd84bc18fd2352cc225ee0d1696` and CI Runs #689 / #162, preserving older runs only as historical evidence if retained.
5. Add an individual ADR-GOVERNANCE-012 five-item checklist section to the Build Report for the eventual closure boundary, clearly distinguishing Producer evidence from CONTROL-owned closure synchronization.

No architecture change, new Task Order, VPS action, V1 action, or later P4 Step is authorized or required by these corrections.

## 14. Acceptance Conditions for Re-Audit

CONTROL will re-audit when the Producer returns:
- revised implementation/tests within the same TO-P4-001 boundary;
- fresh CI evidence for the corrected final branch head;
- revised BR-P4-001 with internally consistent commit/run references;
- explicit evidence for all eight public primitives in the controlled golden system;
- explicit malformed/missing input evidence;
- direct five-table database privilege evidence;
- complete forward-ready hand-off.

Producer must not modify AR-P4-001, CURRENT_CHECKPOINT closure state, Phase/Step lifecycle state, or CONTROL-owned closure synchronization.

## 15. Current Disposition

`AR-P4-001 = REVISE / CORRECTION REQUIRED`

`STEP-P4-001 = ACTIVE / CORRECTION REQUIRED`

`TO-P4-001 = AUTHORIZED TO EXECUTE`

`PH-P4 = ACTIVE / AUTHORIZED`

No Step verification, golden-reference project freeze, Step closure, Phase closure, or G-4 claim is made.

--- STANDING RULES CHECK ---
R1 Continuation: STOPPED(C) — Producer implementation reached its natural hand-off boundary; CONTROL independent audit identified bounded correction items and is now waiting for the Producer correction cycle.
R2 Hand-off: INCLUDED ABOVE — ROL-V2-002 / CONTROL audit disposition and bounded correction request.
R3 Large artifacts: N/A — no new large artifact required for this audit decision.
R4 VPS/SentinelX: N/A — no VPS operation performed.
R5 Quality standard: APPLIED — audit required evidence integrity and coverage beyond minimum passing CI.
G12 Peripheral sync: PENDING AT STEP CLOSURE
Phase/Step: PH-P4 / STEP-P4-001 | Active TO: TO-P4-001
