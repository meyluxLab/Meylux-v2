# AR-P4-003 — CONTROL Independent Re-Verification of Targeted Historical-Volatility Boundary Correction

**Audit Report SID:** `AR-P4-003`  
**Task Order:** `TO-P4-003`  
**Phase / Step:** `PH-P4 / STEP-P4-002`  
**Reviewer:** `ROL-V2-001 — CONTROL / REVIEWER`  
**Status:** APPROVED / VERIFIED  
**Audit basis:** Owner Directive — Targeted Correction and Reverification of STEP-P4-002; TO-P4-003; ADR-GOVERNANCE-012; ADR-GOVERNANCE-013

## 1. Audit boundary

CONTROL independently re-verified only the confirmed `historical_volatility(window=1)` defect and the authorized TO-P4-003 correction boundary. Historical TO-P4-002, BR-P4-002, and AR-P4-002 remain preserved for traceability.

No future Phase 4 Step was implemented or authorized by this audit.

## 2. Authorization and Task Order conformance

TO-P4-003 is the authorized correction Task Order for STEP-P4-002. Its required boundary is explicit rejection of the mathematically invalid `window=1` case before sample-variance division, preservation of valid-window mathematics, direct regression evidence, and full repository regression evidence.

CONTROL confirmed the Producer delivery conforms to that scope.

## 3. Implementation verification

Producer correction commit:

`2c499eccf62ce0f1cc92f1c9dce6ca0f923e1395`

The implementation adds exactly:

```python
if n < 2:
    raise ValueError("window must be at least 2 for historical volatility")
```

immediately after positive-window validation and before annualization/return construction/rolling variance arithmetic.

The valid `window >= 2` sample-log-return formula remains unchanged.

The merged repository contains the same implementation blob as the Producer head:
- Producer blob: `18629d6ca0400316cd24a5434fd0adbd0bf47734`
- merged blob: `18629d6ca0400316cd24a5434fd0adbd0bf47734`

## 4. Regression verification

The required direct regression is:

`test_historical_volatility_window_boundary_is_explicit_and_deterministic`

CONTROL inspected the actual test. It:
- calls `window=1` three times;
- requires the exact deterministic `ValueError`;
- then executes valid `window=2`;
- requires VALID status;
- requires a finite Decimal result;
- requires the result to be non-negative.

This directly covers deterministic boundary rejection and valid-window continuity.

## 5. Actual CI verification

GitHub Actions **CI Core** Run **#758**, Run ID `35427641698`, job `repository-foundation`, Job ID `105856261089`, completed with **SUCCESS**.

The actual final log records:
- `test_controlled_vectors_are_exact` — ok;
- `test_historical_volatility_and_atr_features` — ok;
- `test_historical_volatility_window_boundary_is_explicit_and_deterministic` — ok;
- `Ran 326 tests in 1.032s`;
- `OK`.

The CI checkout used PR #34 merge ref `8ef1d17545a367c1d5a9f188c810704447dd3f2d`. CONTROL independently verified that the four correction-delivery file blobs in the final repository merge commit `4cbeecee0704c7fb2a7b678b8b2f160c8b000b68` are identical to the Producer delivery blobs tested by that merge ref.

## 6. Scope and architecture verification

Comparison of pre-correction integration `c873d43cc4d563802e7aef7f0634deac75239f01` to Producer delivery `d4e4bd14c9809cf3f05a877739f7410ef21b382b` reports exactly four changed files:

1. `src/meylux/quantitative/indicators.py`
2. `tests/test_p4_002_indicators.py`
3. `docs/quantitative/P4_002_INDICATOR_SEMANTICS.md`
4. `docs/build-reports/BR-P4-003.md`

No future P4 Step implementation, architecture change, contract redesign, trading/capital capability, V1 mutation, VPS/SentinelX operation, or unrelated refactor was introduced.

DOC-V2-ARCH-001 remains RATIFIED / FROZEN and no higher-level contract was changed.

## 7. Numerical integrity and temporal/deterministic behavior

The correction removes the identified `n-1=0` divide-by-zero path.

For `window=1`, the public call now deterministically rejects the invalid boundary before arithmetic. No NaN, Inf, fabricated fallback, silent coercion, or divide-by-zero recovery is introduced.

For valid windows, the established natural-log-return sample-volatility and annualization semantics remain unchanged.

No clock, randomness, mutable external state, provider access, I/O, or hidden temporal dependency was introduced.

## 8. Detect → Classify → Route verification

The corrected path is:

**Detect:** `window=1` is an invalid sample-volatility boundary.  
**Classify:** invalid parameter.  
**Route:** deterministic `ValueError` before arithmetic.

This is consistent with the authorized P4-001/P4-002 deterministic invalid-input policy.

## 9. Build Report verification

`BR-P4-003` was read and cross-checked against repository implementation, regression, CI evidence, changed-file comparison, and lifecycle boundaries.

Producer correctly distinguishes IMPLEMENTED / TESTED from project-level VERIFIED / CLOSED and does not perform closure synchronization.

One historical wording discrepancy was identified during audit: the Build Report records the earlier CI Run #757 / pre-report Producer head, while the subsequent Producer delivery communication reports Run #758 / final head. CONTROL relies on the directly inspected Run #758 evidence for final regression verification and does not treat the older Run #757 wording as the final CI record.

## 10. ADR-GOVERNANCE-012 peripheral closure checklist

Before closure synchronization, CONTROL confirms:

1. README must be synchronized with CURRENT_CHECKPOINT.
2. Canonical artifact registry must reflect TO-P4-003 / BR-P4-003 / AR-P4-003 and final STEP-P4-002 state.
3. Specialized test registry must reflect the actual 326-test CI evidence and correction regression.
4. Component registry must reflect the corrected P4-002 implementation evidence and final audit.
5. Database, security, configuration, and observability registries were inspected; the correction changes none of their governed domains, so no fabricated evidence or unrelated status mutation is required.
6. Phase definition and phase registry must reflect final Step state.
7. Change Ledger must record the correction verification and closure synchronization.
8. No supplemental/staging P4-002 registry is present or required.

## 11. Final determination

**DETERMINATION: VERIFIED / CORRECTED / CLOSE AUTHORIZED.**

The confirmed defect is corrected, directly regression-tested, covered by successful full CI, independently inspected for mathematical and numerical correctness, scope-contained, and integrated into the repository Source of Truth.

STEP-P4-002 is therefore eligible to return to **COMPLETE / VERIFIED** through the mandatory CONTROL closure synchronization below.

STEP-P4-003 remains **NOT YET AUTHORIZED**.

## 12. Closure ownership

CONTROL, and CONTROL alone, performs the closure synchronization required by ADR-GOVERNANCE-012. This report does not itself substitute for that synchronization.

**End of Audit Report.**
