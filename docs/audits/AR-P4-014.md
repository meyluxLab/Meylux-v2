# AR-P4-014 — CONTROL Independent Verification — TO-P4-008 Provider-Boundary Correction

Status: APPROVED / VERIFIED — PRODUCER CORRECTION VERIFIED; STEP-P4-006 REMAINS OPEN

Task Order: TO-P4-008
Phase / Step: PH-P4 / STEP-P4-006
Producer Build Report: BR-P4-008
Predecessor Audit: AR-P4-013
Reviewer: ROL-V2-001 — CONTROL / REVIEWER

## 1. Verification disposition

CONTROL independently verified the authorized Producer correction delivered under TO-P4-008.

The correction is VERIFIED at the implementation/test boundary. It resolves the specific P3 normalization blocker identified in AR-P4-013 without changing AcquisitionEnvelope, CanonicalCandle, P3 validation/normalization, Decimal policy, UTC policy, or persistence contracts.

This audit does NOT verify STEP-P4-006, G-4, or Phase-4 closure. Those remain open pending the full governed real-data persistence/runtime/replay/recovery/security evidence boundary.

## 2. Authorization and scope

TO-P4-008 authorizes only:
- minimal additive Binance provider-boundary timeframe preservation;
- direct 15m/1h/4h normalization tests;
- malformed/unsupported interval coverage;
- relevant regression/CI evidence;
- Producer Build Report.

Explicit prohibitions include CanonicalCandle redesign, P3 weakening, direct canonical/quantitative-table insertion, second persistence system, engine reopening, target revision, Phase 5, AI/LLM, trading/capital/custody, V1, and unrelated refactoring.

## 3. Repository evidence

Producer implementation commit:
- a5264d26addc70aab6db125789ef4853ac518d5a
- src/meylux/acquisition/binance.py
- content SHA 32585690e5e7c4d78182302adec50a695c0fde0e

Focused test commit:
- 1f485f5ffcb8616a255a1a0186ed7bca7ba83611
- tests/test_acquisition/test_binance_adapter.py
- content SHA 02666f452645f64d141fcbb4c49d531c1791974a

Final Producer Build Report:
- fb7d9847498323bec067ba98a1e2729f869c0697
- docs/build-reports/BR-P4-008.md
- content SHA c8f6616d2e429a96e35f768357d37253a53e2222

The compare from the implementation commit to the final Build Report commit contains only BR-P4-008.md, confirming no additional implementation change was introduced in the final BR commit.

## 4. Implementation inspection

CONTROL inspected the final Binance adapter implementation and confirmed:

1. fetch_klines() validates Binance-specific supported intervals before network use.
2. The requested interval is retained as provider-specific kline interval context.
3. The original Binance REST row remains under payload["row"].
4. The existing Binance P3 mapping shape payload["k"] is populated with the candle fields and explicit interval at payload["k"]["i"].
5. payload["k"]["x"] is derived from the Binance close boundary against the adapter UTC receipt clock; historical candles are not unconditionally marked closed.
6. source_sequence remains the Binance kline open-time value.
7. Provider/instrument/provenance/event semantics remain unchanged.
8. No AcquisitionEnvelope or CanonicalCandle source change was made.
9. No second timeframe contract or persistence path was introduced.

CONTROL also inspected the existing P3 normalization mapping and confirmed it already consumes Binance payload["k"]["i"] as the authoritative timeframe field. Therefore the correction restores the intended existing mapping rather than changing P3 semantics.

## 5. Direct test verification

On the synchronized CONTROL VPS checkout at:
- /srv/meylux-v2
- checkout fb7d9847498323bec067ba98a1e2729f869c0697

CONTROL executed with SentinelX:
- Binance adapter suite: 19 tests — OK.
- P3-005 normalization suite: 16 tests — OK.
- Full repository unittest discovery: 448 tests in 2.609s — OK.

A focused live-path test was also executed:
- 15m, 1h, and 4h real Binance Spot kline requests;
- 116, 31, and 9 envelopes respectively;
- every envelope normalized successfully through the existing P3 normalize() implementation;
- resulting canonical timeframes were exactly 15m, 1h, and 4h.

Observed live-data normalization result:
- 15m: 116 acquired, 115 closed at the execution receipt boundary;
- 1h: 31 acquired, 30 closed;
- 4h: 9 acquired, 8 closed.

The single open candle in each requested interval was not silently promoted to closed state.

## 6. CI evidence

BR-P4-008 records CI Core Run #1049:
- Run ID 35476114191
- Job ID 105985533666
- SUCCESS
- 448 full repository tests
- relevant Binance/P2/P3 suites passed.

CONTROL's independent VPS execution reproduced the 448-test repository result locally. The repository's CI record is therefore corroborated by an independent execution, although the CI service log itself was not separately fetched in this audit.

## 7. Architecture and contract decision

VERIFIED:
- existing Binance provider boundary preserved;
- AcquisitionEnvelope unchanged;
- CanonicalCandle unchanged;
- P3 structural validation unchanged;
- P3 temporal validation unchanged;
- P3 normalization unchanged;
- P2 raw/staging boundary unchanged;
- CanonicalPersistence unchanged;
- Decimal and UTC policies unchanged;
- source/provenance/identity semantics preserved.

No architecture conflict was identified.

## 8. Real-data boundary after correction

The previous normalization blocker is resolved.

CONTROL has independently demonstrated:
BinanceAdapter -> P3 normalization
for real BTCUSDT 15m/1h/4h candles.

However, the complete governed path:
BinanceAdapter -> P2 RawStagingRepository -> P3 validation/normalization/quality -> CanonicalPersistence -> P4 orchestration -> persistence -> replay/recovery
has NOT yet been fully executed after correction.

The VPS currently has no accessible MEYLUX_DB_NAME / MEYLUX_DB_PASSWORD / MEYLUX_APP_PASSWORD environment values, host-level PostgreSQL/Redis services are inactive, and Docker socket access is denied to the current CONTROL command context. Therefore CONTROL did not fabricate, inject, or bypass the authoritative persistence boundary.

This is an execution-environment boundary, not an implementation defect in TO-P4-008.

## 9. Performance boundary

No Producer or CONTROL engine modification was performed under TO-P4-008.

Previously verified Decision-B measurements remain unchanged:
- 21 primary + 25 1H + 7 4H: 3.766 ms
- 116 primary + 31 1H + 9 4H: 26.078 ms
- 1000 primary + 252 1H + 64 4H: 704.670 ms

No EMA, Market Structure, Decimal policy, or roadmap target was reopened.

## 10. Findings

Finding F1 — CLOSED / VERIFIED:
Historical Binance kline normalization lacked explicit timeframe context at the provider boundary. TO-P4-008 adds the required provider-boundary context while preserving the existing P3 mapping. Direct tests and live real-data normalization confirm resolution.

Finding F2 — OPEN:
Full governed real-data persistence/runtime/replay/recovery evidence remains incomplete because the current VPS execution environment does not expose the required runtime/database boundary to CONTROL.

Finding F3 — OPEN:
STEP-P4-006/G-4 cannot be closed until the full controlled BTCUSDT 15M + 1H + 4H evidence basis is executed through CanonicalPersistence and the authorized P4 runtime/replay/recovery/security surfaces.

## 11. Lifecycle

BR-P4-008: VERIFIED by this audit.
TO-P4-008: implementation boundary VERIFIED; Task Order remains active until the remaining authorized evidence boundary is completed or separately closed by CONTROL.
STEP-P4-006: ACTIVE / AUTHORIZED.
G-4: NOT ESTABLISHED.
Phase 5: NOT AUTHORIZED.
Phase 4: ACTIVE / AUTHORIZED.

## 12. Closure synchronization

This audit is not Step closure. Therefore full ADR-GOVERNANCE-012 Closure Synchronization is intentionally NOT executed.

The five closure surfaces remain pending final Step closure:
1. README status reconciliation;
2. artifacts.yaml lifecycle/status reconciliation;
3. specialized registry reconciliation;
4. standalone status-bearing document reconciliation;
5. supplemental/staging registry retirement/absorption.

CONTROL retains sole ownership of that final closure cycle.
